# VulnRisk: 基于机器学习的软件漏洞风险评估平台
## 详细项目报告

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [机器学习模型详细设计](#3-机器学习模型详细设计)
4. [模型训练过程详解](#4-模型训练过程详解)
5. [模型输出详解](#5-模型输出详解)
6. [风险评分算法](#6-风险评分算法)
7. [系统实现](#7-系统实现)
8. [实验评估与结果](#8-实验评估与结果)
9. [系统特性与创新点](#9-系统特性与创新点)
10. [局限性与未来工作](#10-局限性与未来工作)
11. [结论](#11-结论)

---

## 1. 项目概述

### 1.1 项目背景

随着软件系统的复杂性和规模不断增长，安全漏洞的数量和严重程度也在持续上升。传统的漏洞评估方法主要依赖人工分析，效率低下且难以应对大规模漏洞数据。因此，开发自动化的漏洞风险评估系统具有重要的实用价值。

### 1.2 项目目标

本项目旨在构建一个基于机器学习的漏洞风险评估平台，实现以下目标：

1. **自动化漏洞识别**: 基于文本描述自动识别潜在漏洞
2. **风险量化评估**: 结合机器学习预测和CVSS评分计算综合风险
3. **可扩展架构**: 支持多种模型类型和批量处理
4. **用户友好界面**: 提供直观的可视化和管理界面

### 1.3 技术栈

- **前端**: Vue 3 + TypeScript + Element Plus + ECharts
- **后端**: Node.js + Express + Prisma + PostgreSQL
- **机器学习服务**: Python + Flask + scikit-learn + XGBoost + imbalanced-learn
- **部署**: Docker + Docker Compose

---

## 2. 系统架构

### 2.1 整体架构

系统采用微服务架构，包含三个核心服务：

```
┌─────────────┐
│   Frontend  │  Vue 3 + TypeScript (Port 80)
│   (Nginx)   │
└──────┬──────┘
       │ HTTP/REST API
┌──────▼──────┐
│   Backend   │  Node.js + Express (Port 3000)
│   Service   │
└──┬────────┬─┘
   │        │
   │        │ HTTP API
   │        │
┌──▼────────▼──┐      ┌──────────────┐
│  PostgreSQL  │      │  ML Service  │  Python + Flask (Port 5000)
│  Database    │      │              │
└──────────────┘      └──────────────┘
```

### 2.2 核心组件

#### 2.2.1 前端层 (Frontend Layer)
- **技术栈**: Vue 3, TypeScript, Element Plus, ECharts
- **功能模块**:
  - 数据集管理界面
  - 模型训练与激活
  - 预测结果可视化
  - GitHub文本抓取集成
  - 批量预测处理

#### 2.2.2 后端层 (Backend Layer)
- **技术栈**: Node.js, Express, Prisma ORM, PostgreSQL
- **核心功能**:
  - RESTful API服务
  - JWT身份认证
  - 数据持久化
  - 文件上传处理
  - ML服务代理

#### 2.2.3 机器学习服务层 (ML Service Layer)
- **技术栈**: Python, Flask, scikit-learn, XGBoost, imbalanced-learn
- **核心功能**:
  - 模型训练
  - 单样本/批量预测
  - 风险评分计算
  - CVSS相似度估算

---

## 3. 机器学习模型详细设计

### 3.1 问题定义

将漏洞风险评估问题建模为**二分类问题**：

- **正类 (y=1)**: 高风险漏洞 (CVSS ≥ 7.0)
- **负类 (y=0)**: 低风险漏洞 (CVSS < 7.0)

**输入特征**: 漏洞文本描述（自然语言）
**输出**: 漏洞概率 P(vuln) ∈ [0, 1]

### 3.2 数据预处理

#### 3.2.1 文本预处理流程

```python
1. 文本转小写 (lowercase)
2. 移除特殊字符 (保留字母、数字、空格)
3. 分词处理 (tokenization)
4. 停用词移除 (English stop words)
5. 文本标准化 (normalization)
```

**具体实现**:
```python
import re

def preprocess_text(text):
    text_lower = str(text).lower()
    text_clean = re.sub(r'[^a-z0-9\s]', ' ', text_lower)
    text_clean = ' '.join(text_clean.split())
    return text_clean
```

#### 3.2.2 特征工程

**TF-IDF向量化** (Term Frequency-Inverse Document Frequency):

- **最大特征数**: 20,000
- **N-gram范围**: (1, 2) - 支持单字和双字组合
- **最小文档频率**: 2 - 过滤罕见词
- **最大文档频率**: 0.95 - 过滤常见词
- **停用词列表**: 英文停用词

**数学表示**:
```
TF-IDF(t, d) = TF(t, d) × IDF(t)

其中:
- TF(t, d) = 词t在文档d中的频率
- IDF(t) = log(N / df(t))
- N = 总文档数
- df(t) = 包含词t的文档数
```

**实现代码**:
```python
vectorizer = TfidfVectorizer(
    max_features=20000,
    stop_words='english',
    ngram_range=(1, 2),
    min_df=2,
    max_df=0.95,
    lowercase=True
)
X = vectorizer.fit_transform(processed_texts)
```

### 3.3 模型选择

系统支持两种集成学习算法：

#### 3.3.1 XGBoost (Extreme Gradient Boosting)

**算法原理**: 基于梯度提升决策树的集成学习算法

**核心参数**:
```python
{
    'n_estimators': 100,        # 树的数量
    'max_depth': 6,             # 树的最大深度
    'learning_rate': 0.1,       # 学习率
    'scale_pos_weight': auto,    # 类别不平衡权重（自动计算）
    'random_state': 42,         # 随机种子
    'eval_metric': 'logloss',    # 评估指标
    'use_label_encoder': False   # 不使用标签编码器
}
```

**类别权重计算**:
```python
scale_pos_weight = count(negative) / count(positive)
# 例如：如果负类131,612个，正类40,607个
# scale_pos_weight = 131612 / 40607 ≈ 3.24
```

**优势**:
- 处理非线性关系能力强
- 内置正则化防止过拟合
- 支持类别不平衡处理
- 训练速度快
- 特征重要性分析

#### 3.3.2 Random Forest (随机森林)

**算法原理**: 基于Bagging的集成学习算法

**核心参数**:
```python
{
    'n_estimators': 100,         # 树的数量
    'max_depth': None,           # 不限制深度
    'class_weight': 'balanced',  # 自动平衡类别权重
    'random_state': 42,          # 随机种子
    'n_jobs': -1                 # 并行计算（使用所有CPU核心）
}
```

**类别权重计算**:
```python
class_weight = {
    0: n_samples / (2 * n_class0), 
    1: n_samples / (2 * n_class1)
}
```

**优势**:
- 对过拟合鲁棒性强
- 特征重要性分析
- 训练过程并行化
- 对噪声数据不敏感

### 3.4 类别不平衡处理

#### 3.4.1 数据分布情况

根据实际训练数据统计：
- **总样本数**: 172,219
- **低风险样本 (y=0)**: 131,612 (76.42%)
- **高风险样本 (y=1)**: 40,607 (23.58%)
- **不平衡比例**: 3.24:1

#### 3.4.2 SMOTE (Synthetic Minority Oversampling Technique)

**问题**: 高风险漏洞样本通常远少于低风险样本

**解决方案**: SMOTE过采样技术

**算法流程**:
```
1. 对少数类样本，找到k个最近邻
2. 在最近邻之间随机插值生成新样本
3. 重复直到类别平衡
```

**实现参数**:
```python
{
    'k_neighbors': min(5, n_minority - 1),  # 自适应k值
    'random_state': 42
}
```

**注意**: SMOTE仅在训练集上应用，测试集保持原始分布

**使用场景**:
- 类别不平衡比例 > 3:1
- 少数类样本数 > 10（SMOTE需要至少2个样本）

#### 3.4.3 类别权重调整

**XGBoost**: 使用 `scale_pos_weight` 参数
```python
scale_pos_weight = count(negative) / count(positive)
```

**Random Forest**: 使用 `class_weight='balanced'`
```python
class_weight = {0: n_samples / (2 * n_class0), 
                1: n_samples / (2 * n_class1)}
```

### 3.5 概率校准

#### 3.5.1 校准必要性

原始模型输出的概率可能不够准确，需要进行校准以获得更可靠的概率估计。未校准的模型可能输出过于自信或过于保守的概率值。

#### 3.5.2 校准方法

**Isotonic Regression (等渗回归)**:

```python
CalibratedClassifierCV(
    base_estimator=model,
    method='isotonic',
    cv=3  # 3折交叉验证
)
```

**原理**: 使用保序回归将原始概率映射到校准后的概率，确保概率单调性。

**优势**:
- 非参数方法，适应性强
- 保持概率顺序
- 提高概率估计准确性
- 通过交叉验证避免过拟合

**校准过程**:
1. 使用3折交叉验证
2. 在每个折上训练校准模型
3. 最终模型是多个校准模型的集成

---

## 4. 模型训练过程详解

### 4.1 训练数据准备

#### 4.1.1 数据来源

- **CVE数据库**: Common Vulnerabilities and Exposures
- **数据格式**: CSV/JSON
- **字段**: `cve_id`, `description_clean`, `cvss_base`, `cvss_severity`, `label`

#### 4.1.2 数据统计

根据实际训练数据（`preprocessing_report.json`）：

**总体统计**:
- **总样本数**: 172,219
- **有效CVSS评分**: 94,696 (55.0%)
- **缺失CVSS评分**: 77,523 (45.0%)

**文本描述统计**:
- **平均长度**: 338.8 字符
- **最小长度**: 0 字符
- **最大长度**: 3,997 字符
- **空文本数**: 12,159

**CVSS评分分布**:
- **MEDIUM**: 48,976 (51.7%)
- **HIGH**: 33,111 (35.0%)
- **CRITICAL**: 7,496 (7.9%)
- **LOW**: 5,086 (5.4%)
- **NONE**: 27 (0.03%)

**CVSS评分统计**:
- **平均值**: 6.59
- **最小值**: 0.0
- **最大值**: 10.0

#### 4.1.3 标签构建

系统自动从CVSS评分构建二分类标签：

```python
# 规则1：使用cvss_base
if cvss_base >= 7.0:
    label = 1  # 高风险
else:
    label = 0  # 低风险

# 规则2：如果cvss_base缺失，使用cvss_severity
if cvss_severity in ['HIGH', 'CRITICAL']:
    label = 1
elif cvss_severity in ['MEDIUM', 'LOW', 'NONE']:
    label = 0
```

**最终标签分布**:
- **低风险 (y=0)**: 131,612 (76.42%)
- **高风险 (y=1)**: 40,607 (23.58%)
- **不平衡比例**: 3.24:1

### 4.2 训练流程

#### 4.2.1 完整训练步骤

```
步骤1: 加载和预处理数据
   ↓
步骤2: 特征工程（TF-IDF向量化）
   ↓
步骤3: 数据集划分（80/20）
   ↓
步骤4: SMOTE过采样（可选）
   ↓
步骤5: 模型训练
   ↓
步骤6: 概率校准
   ↓
步骤7: 模型评估
   ↓
步骤8: 模型保存
```

#### 4.2.2 详细训练过程

**步骤1: 数据加载和预处理**

```python
# 加载CSV数据
df = load_cve_dataset(csv_path)

# 预处理和构建标签
df = preprocess_dataset(df, text_column='description_clean')

# 过滤有效标签
df_valid = df[df['y'] != -1].copy()
```

**步骤2: 特征工程**

```python
# 构建TF-IDF向量化器
vectorizer = TfidfVectorizer(
    max_features=20000,
    stop_words='english',
    ngram_range=(1, 2),
    min_df=2,
    max_df=0.95,
    lowercase=True
)

# 向量化文本
X = vectorizer.fit_transform(processed_texts)
# 输出形状: (172219, 20000)
```

**步骤3: 数据集划分**

```python
X_train, X_test, y_train, y_test = train_test_split(
    X, y, 
    test_size=0.2,           # 20%作为测试集
    random_state=42,          # 确保可复现
    stratify=y                # 分层采样，保持类别分布
)

# 训练集大小: 137,775
# 测试集大小: 34,444
```

**步骤4: SMOTE过采样（可选）**

```python
if use_smote:
    n_minority = np.sum(y_train == 1)
    k_neighbors = min(5, n_minority - 1)
    
    smote = SMOTE(random_state=42, k_neighbors=k_neighbors)
    X_train, y_train = smote.fit_resample(X_train, y_train)
    
    # SMOTE后训练集大小: 约260,000+（取决于原始分布）
    # 类别分布: [130000, 130000]（平衡）
```

**步骤5: 模型训练**

**XGBoost训练**:
```python
base_model = xgb.XGBClassifier(
    n_estimators=100,
    max_depth=6,
    learning_rate=0.1,
    random_state=42,
    scale_pos_weight=3.24,  # 自动计算
    eval_metric='logloss',
    use_label_encoder=False
)

# 训练
model.fit(X_train, y_train)
```

**Random Forest训练**:
```python
base_model = RandomForestClassifier(
    n_estimators=100,
    max_depth=None,
    random_state=42,
    n_jobs=-1,
    class_weight='balanced'
)

# 训练
model.fit(X_train, y_train)
```

**步骤6: 概率校准**

```python
if calibrate:
    model = CalibratedClassifierCV(
        base_estimator=base_model,
        method='isotonic',
        cv=3  # 3折交叉验证
    )
    model.fit(X_train, y_train)
```

**步骤7: 模型评估**

```python
# 预测
y_pred = model.predict(X_test)
y_pred_proba = model.predict_proba(X_test)[:, 1]

# 计算指标
metrics = {
    'accuracy': accuracy_score(y_test, y_pred),
    'precision': precision_score(y_test, y_pred),
    'recall': recall_score(y_test, y_pred),
    'f1': f1_score(y_test, y_pred),
    'roc_auc': roc_auc_score(y_test, y_pred_proba)
}
```

**步骤8: 模型保存**

```python
# 保存模型文件
joblib.dump(model, 'model.joblib')
joblib.dump(vectorizer, 'vectorizer.joblib')

# 保存元数据
metadata = {
    'model_type': 'XGBoost',
    'params': {...},
    'metrics': {...},
    'feature_importance': [...],
    'class_distribution': {...},
    'trained_at': datetime.now().isoformat()
}
json.dump(metadata, open('metadata.json', 'w'))
```

### 4.3 训练输出

#### 4.3.1 控制台输出示例

```
============================================================
步骤1: 加载和预处理数据
============================================================
正在加载数据集: ./merged_json_table.csv
数据集形状: (172219, 15)
列名: ['cve_id', 'description_clean', 'cvss_base', ...]

开始预处理数据集...
处理缺失值...
构建风险标签...

标签分布:
  高风险 (y=1): 40607 (23.58%)
  低风险 (y=0): 131612 (76.42%)
  缺失标签: 0

============================================================
步骤2: 特征工程
============================================================
构建特征工程管道...
文本列: description_clean
最大特征数: 20000
特征矩阵形状: (172219, 20000)
特征数量: 20000

============================================================
步骤3: 数据集划分
============================================================
训练集大小: 137775
测试集大小: 34444

============================================================
步骤4: 应用SMOTE处理类别不平衡
============================================================
SMOTE后训练集大小: (260000, 20000), 类别分布: [130000 130000]

============================================================
步骤5: 模型训练
============================================================
训练XGBoost模型...
应用概率校准 (isotonic)...
开始训练...
训练完成！

性能指标:
  accuracy: 0.8469
  precision: 0.7762
  recall: 0.4929
  f1: 0.6030
  roc_auc: 0.8875

============================================================
步骤6: 保存模型
============================================================
模型已保存: ./models/risk_model_001/model.joblib
向量化器已保存: ./models/risk_model_001/vectorizer.joblib
元数据已保存: ./models/risk_model_001/metadata.json
预处理报告已保存: ./models/risk_model_001/preprocessing_report.json
训练预测结果已保存: ./models/risk_model_001/training_predictions.csv

============================================================
训练完成！
============================================================
模型输出目录: ./models/risk_model_001
```

#### 4.3.2 保存的文件

训练完成后，模型目录包含以下文件：

```
models/risk_model_001/
├── model.joblib              # 训练好的模型（包含校准）
├── vectorizer.joblib         # TF-IDF向量化器
├── metadata.json             # 模型元数据和性能指标
├── preprocessing_report.json # 数据预处理报告
└── training_predictions.csv  # 训练集预测结果
```

#### 4.3.3 特征重要性

模型会输出Top 20最重要的特征：

```json
{
  "feature_importance": [
    {"name": "code execution", "importance": 0.0435},
    {"name": "reflected xss", "importance": 0.0238},
    {"name": "chrome", "importance": 0.0197},
    {"name": "site scripting", "importance": 0.0197},
    {"name": "execute code", "importance": 0.0156},
    ...
  ]
}
```

这些特征反映了模型认为对识别高风险漏洞最重要的关键词和短语。

---

## 5. 模型输出详解

### 5.1 预测流程

```
用户输入文本
   ↓
文本预处理（小写、去特殊字符、分词）
   ↓
TF-IDF向量化
   ↓
模型预测 → P(vuln_raw) [原始概率]
   ↓
置信度调整（基于特征覆盖度）
   ↓
P(vuln) [调整后的概率]
   ↓
CVSS估算（如需要）
   ↓
风险评分计算
   ↓
风险等级映射
   ↓
适用性评估（Business Clipping）
   ↓
返回结果
```

### 5.2 输出字段详解

#### 5.2.1 核心预测字段

**1. p_vuln_raw (原始模型预测值)**
- **类型**: float [0, 1]
- **说明**: 模型直接输出的高风险漏洞概率，未经任何调整
- **用途**: 保留原始预测值，用于分析和调试
- **示例**: `0.8523` 表示模型认为有85.23%的概率是高风险漏洞

**2. p_vuln (调整后的预测值)**
- **类型**: float [0, 1]
- **说明**: 经过置信度调整后的概率值
- **调整规则**:
  - 如果特征覆盖度 < 0.005 或文本 < 10字符: `p_vuln = min(0.15, p_vuln_raw * 0.1)`
  - 如果特征覆盖度 < 0.02 且无安全关键词: `p_vuln = min(0.25, p_vuln_raw * 0.2)`
  - 如果特征覆盖度 < 0.05 且无安全关键词: `p_vuln = min(0.35, p_vuln_raw * 0.4)`
  - 如果特征权重总和 < 0.1 且无安全关键词: `p_vuln = min(0.30, p_vuln_raw * 0.3)`
  - 如果特征覆盖度 < 0.05 但有安全关键词: `p_vuln = p_vuln_raw * 0.7`
  - 其他情况: `p_vuln = p_vuln_raw`
- **用途**: 用于风险评分计算

**3. risk_score (综合风险评分)**
- **类型**: float [0, 1]
- **说明**: 结合P(vuln)和CVSS评分的综合风险评分
- **计算公式**:
  ```
  如果存在CVSS评分:
      RiskScore = α × P(vuln) + (1 - α) × (CVSS / 10.0)
  否则:
      RiskScore = P(vuln)
  
  其中 α = 0.6（默认值）
  ```
- **示例**: `0.8234` 表示综合风险评分为82.34%

**4. risk_level (风险等级)**
- **类型**: string
- **可能值**: `"Low"`, `"Medium"`, `"High"`, `"Critical"`, `"Unknown"`
- **映射规则**:
  ```
  RiskScore < 0.4  → "Low"
  0.4 ≤ RiskScore < 0.6  → "Medium"
  0.6 ≤ RiskScore < 0.8  → "High"
  RiskScore ≥ 0.8  → "Critical"
  ```
- **特殊情况**: 如果适用性评估失败，可能返回 `"Unknown"` 或 `"Low"`

#### 5.2.2 CVSS相关字段

**5. cvss_base_score (CVSS基础评分)**
- **类型**: float [0, 10] 或 null
- **说明**: CVSS基础评分，可能来自：
  - 用户输入
  - 相似度估算
  - 概率映射（备用方法）
- **示例**: `7.5` 表示CVSS评分为7.5

**6. cvss_estimated (CVSS是否估算)**
- **类型**: boolean
- **说明**: 标记CVSS评分是否为系统估算值
- **true**: CVSS是系统估算的
- **false**: CVSS是用户提供的

**7. cvss_method (CVSS来源方法)**
- **类型**: string
- **可能值**:
  - `"user_provided"`: 用户提供
  - `"similarity"`: 基于相似度估算
  - `"p_vuln_fallback"`: 基于概率映射（备用）
  - `"similarity_failed"`: 相似度估算失败
  - `"not_applicable"`: 不适用

#### 5.2.3 可靠性相关字段

**8. reliability (可靠性等级)**
- **类型**: string
- **可能值**: `"High"`, `"Medium"`, `"Low"`
- **判定规则**:
  ```
  如果 p_vuln_raw < 0.1 或 feature_coverage < 0.02:
      reliability = "Low"
  如果 p_vuln_raw < 0.3 或 feature_coverage < 0.05:
      reliability = "Medium"
  否则:
      reliability = "High"
  ```
- **用途**: 指示预测结果的可靠程度

**9. is_clipped (是否被裁剪/降级)**
- **类型**: boolean
- **说明**: 标记预测结果是否因适用性评估失败而被降级
- **true**: 结果被降级，可靠性降低
- **false**: 正常预测结果

**10. warnings (警告信息列表)**
- **类型**: array of strings
- **说明**: 包含关于预测结果的警告信息
- **常见警告**:
  - `"Input text is too short. Evidence insufficient for reliable scoring."`
  - `"Input text has low similarity to vulnerability corpus. Confidence reduced."`
  - `"Input text has insufficient TF-IDF features. Evidence insufficient for reliable scoring."`
  - `"Model probability is very low. Insufficient evidence for high-severity scoring."`
  - `"Low model confidence due to limited evidence. Results should be interpreted with caution."`

#### 5.2.4 调试信息字段

**11. feature_coverage (特征覆盖度)**
- **类型**: float [0, 1]
- **说明**: 非零TF-IDF特征占总特征的比例
- **计算公式**: `nonzero_features / total_features`
- **用途**: 衡量输入文本与训练数据的相似度

**12. feature_sum (特征权重总和)**
- **类型**: float
- **说明**: 所有TF-IDF特征值的总和
- **用途**: 衡量文本在特征空间中的强度

**13. has_security_keywords (是否包含安全关键词)**
- **类型**: boolean
- **说明**: 文本中是否包含常见的安全相关关键词
- **关键词列表**: vulnerability, vuln, security, exploit, attack, injection, sql, xss, csrf, buffer, overflow, authentication, authorization, privilege, escalation, traversal, disclosure, execution, bypass, denial, service, dos, ddos, malicious, unauthorized, access, exposure, leak, breach, penetration, intrusion, hack, crack

**14. confidence_adjustment (置信度调整系数)**
- **类型**: float
- **说明**: 应用于原始预测值的调整系数
- **用途**: 用于调试，了解预测值被调整的程度

#### 5.2.5 元数据字段

**15. meta (元数据对象)**
- **类型**: object
- **包含字段**:
  ```json
  {
    "applicable": true/false,           // 是否适用于风险评分
    "reason": "LOW_SIMILARITY" | ...,   // 如果不适用，说明原因
    "max_similarity": 0.65,             // 最大相似度值
    "nonzero_features": 45,             // 非零特征数
    "text_len": 185,                    // 文本长度
    "thresholds": {                     // 阈值配置
      "clip_pvuln_threshold": 0.10,
      "clip_sim_threshold": 0.18,
      "clip_min_text_len": 20,
      "clip_min_nonzero_tfidf": 3
    }
  }
  ```

**16. explanation (解释信息)**
- **类型**: string 或 null
- **说明**: 对预测结果的文字解释
- **内容**: 通常说明置信度低的原因，强调"low confidence"、"insufficient evidence"等，不否定漏洞相关性

### 5.3 输出示例

#### 5.3.1 正常预测示例

**输入**:
```json
{
  "sample_id": "CVE-2024-0001",
  "text_description": "A remote code execution vulnerability exists in the web server component. An attacker can exploit this by sending a specially crafted HTTP request.",
  "cvss_base_score": 9.8
}
```

**输出**:
```json
{
  "sample_id": "CVE-2024-0001",
  "p_vuln": 0.9234,
  "p_vuln_raw": 0.9234,
  "risk_score": 0.9123,
  "risk_level": "Critical",
  "cvss_base_score": 9.8,
  "cvss_estimated": false,
  "cvss_method": "user_provided",
  "is_clipped": false,
  "reliability": "High",
  "warnings": [],
  "feature_coverage": 0.045,
  "feature_sum": 2.34,
  "has_security_keywords": true,
  "confidence_adjustment": 1.0,
  "explanation": null,
  "meta": {
    "applicable": true,
    "reason": null,
    "max_similarity": 0.78,
    "nonzero_features": 900,
    "text_len": 185,
    "thresholds": {
      "clip_pvuln_threshold": 0.10,
      "clip_sim_threshold": 0.18,
      "clip_min_text_len": 20,
      "clip_min_nonzero_tfidf": 3
    }
  }
}
```

#### 5.3.2 低置信度预测示例

**输入**:
```json
{
  "sample_id": "test_xss_001",
  "text_description": "Cross-site scripting (XSS) vulnerability allows an attacker to inject malicious scripts into web pages viewed by other users."
}
```

**输出**:
```json
{
  "sample_id": "test_xss_001",
  "p_vuln": 0.15,
  "p_vuln_raw": 0.15,
  "risk_score": 0.15,
  "risk_level": "Low",
  "cvss_base_score": null,
  "cvss_estimated": false,
  "cvss_method": "similarity_failed",
  "is_clipped": true,
  "reliability": "Low",
  "warnings": [
    "Model probability is very low (0.150 < 0.100). Insufficient evidence for high-severity scoring."
  ],
  "feature_coverage": 0.002,
  "feature_sum": 0.05,
  "has_security_keywords": true,
  "confidence_adjustment": 1.0,
  "explanation": "Model probability is low (0.150). The assessment has low confidence. Results are for reference only and should be interpreted with caution.",
  "meta": {
    "applicable": false,
    "reason": "LOW_PVULN",
    "max_similarity": 0.12,
    "nonzero_features": 45,
    "text_len": 185,
    "thresholds": {
      "clip_pvuln_threshold": 0.10,
      "clip_sim_threshold": 0.18,
      "clip_min_text_len": 20,
      "clip_min_nonzero_tfidf": 3
    }
  }
}
```

### 5.4 置信度调整机制

系统实现了多层次的置信度调整机制，以防止对无关文本或低质量输入产生过度自信的预测：

#### 5.4.1 调整规则

1. **极低特征覆盖度或极短文本**
   - 条件: `feature_coverage < 0.005` 或 `text_length < 10`
   - 调整: `p_vuln = min(0.15, p_vuln_raw * 0.1)`
   - 说明: 文本与训练数据差异极大，大幅降低置信度

2. **低特征覆盖度且无安全关键词**
   - 条件: `feature_coverage < 0.02` 且 `has_security_keywords = false`
   - 调整: `p_vuln = min(0.25, p_vuln_raw * 0.2)`
   - 说明: 可能是无关文本，显著降低置信度

3. **中等特征覆盖度且无安全关键词**
   - 条件: `feature_coverage < 0.05` 且 `has_security_keywords = false`
   - 调整: `p_vuln = min(0.35, p_vuln_raw * 0.4)`
   - 说明: 适度降低置信度

4. **低特征权重总和且无安全关键词**
   - 条件: `feature_sum < 0.1` 且 `has_security_keywords = false`
   - 调整: `p_vuln = min(0.30, p_vuln_raw * 0.3)`
   - 说明: 文本在特征空间中很弱，降低置信度

5. **低特征覆盖度但有安全关键词**
   - 条件: `feature_coverage < 0.05` 且 `has_security_keywords = true`
   - 调整: `p_vuln = p_vuln_raw * 0.7`
   - 说明: 虽然特征覆盖度低，但有安全相关关键词，适度降低

6. **正常情况**
   - 条件: 不满足上述任何条件
   - 调整: `p_vuln = p_vuln_raw`
   - 说明: 使用原始预测值

#### 5.4.2 适用性评估（Business Clipping）

系统实现了"工程裁剪"机制，对明显不适用的输入进行标记：

**裁剪条件**:
1. **文本过短**: `text_length < 20`
2. **相似度过低**: `max_similarity < 0.18` 且用户未提供CVSS
3. **信号太弱**: `nonzero_features < 3` 且用户未提供CVSS
4. **概率极低**: `p_vuln < 0.10` 且相似度低且用户未提供CVSS

**裁剪效果**:
- `is_clipped = true`
- `reliability = "Low"`
- 添加相应的警告信息
- `risk_level` 可能被设置为 `"Low"` 或 `"Unknown"`

---

## 6. 风险评分算法

### 6.1 风险评分公式

系统采用**加权融合**方法，结合机器学习预测概率和CVSS评分：

#### 6.1.1 基本公式

**当CVSS评分存在时**:
```
RiskScore = α × P(vuln) + (1 - α) × CVSS_norm
```

**当CVSS评分缺失时**:
```
RiskScore = P(vuln)
```

**参数说明**:
- `P(vuln)`: 模型预测的漏洞概率 [0, 1]
- `CVSS_norm`: 归一化的CVSS评分 = CVSS_base / 10.0 [0, 1]
- `α`: 权重参数，默认值 0.6

**设计理念**:
- `α = 0.6`: 更重视机器学习预测，因为模型能从文本中提取复杂模式
- `(1 - α) = 0.4`: CVSS评分提供专家知识补充
- 两者结合能够平衡数据驱动和领域知识

#### 6.1.2 数学性质

**边界条件**:
- 当 `P(vuln) = 0` 且 `CVSS_norm = 0` 时，`RiskScore = 0`
- 当 `P(vuln) = 1` 且 `CVSS_norm = 1` 时，`RiskScore = 1`

**单调性**:
- `RiskScore` 关于 `P(vuln)` 和 `CVSS_norm` 都是单调递增的

**连续性**:
- 函数在所有定义域内连续

### 6.2 CVSS评分估算

当用户未提供CVSS评分时，系统提供两种估算方法：

#### 6.2.1 基于相似度的CVSS估算（优先方法）

**算法**: 基于TF-IDF余弦相似度的k近邻加权平均

**流程**:
```
1. 对输入文本进行TF-IDF向量化
2. 计算与训练数据所有样本的余弦相似度
3. 找到top-k最相似的样本 (默认k=5)
4. 过滤相似度 < 0.1 的样本
5. 对剩余样本的CVSS值进行加权平均
   权重 = 相似度值
```

**数学表示**:
```
CVSS_estimated = Σ(sim_i × CVSS_i) / Σ(sim_i)

其中:
- sim_i: 第i个相似样本的相似度
- CVSS_i: 第i个相似样本的CVSS评分
- 求和范围: top-k相似样本且sim_i > 0.1
```

**阈值机制**:
- **相似度阈值**: 0.18
- 如果最大相似度 < 0.18，不进行估算（避免无关文本被错误匹配）

**优势**:
- 利用历史数据的CVSS信息
- 基于文本语义相似度
- 加权平均减少异常值影响

#### 6.2.2 基于概率的CVSS估算（备用方法）

当相似度估算失败时，使用分段线性映射：

```python
if P(vuln) < 0.2:
    CVSS = (P(vuln) / 0.2) × 2.0
elif P(vuln) < 0.4:
    CVSS = 2.0 + ((P(vuln) - 0.2) / 0.2) × 2.0
elif P(vuln) < 0.6:
    CVSS = 4.0 + ((P(vuln) - 0.4) / 0.2) × 2.0
elif P(vuln) < 0.8:
    CVSS = 6.0 + ((P(vuln) - 0.6) / 0.2) × 1.5
elif P(vuln) < 0.9:
    CVSS = 7.5 + ((P(vuln) - 0.8) / 0.1) × 1.5
else:
    CVSS = 9.0 + ((P(vuln) - 0.9) / 0.1) × 1.0
```

**设计思路**: 将概率区间映射到对应的CVSS评分区间，保持单调性。

### 6.3 风险等级划分

根据风险评分，将漏洞划分为四个等级：

| Risk Score | Risk Level | 说明 |
|------------|------------|------|
| [0, 0.40)  | Low        | 低风险，可延后处理 |
| [0.40, 0.70) | Medium   | 中等风险，需要关注 |
| [0.70, 0.90) | High     | 高风险，优先处理 |
| [0.90, 1.0]  | Critical | 严重风险，立即处理 |

**阈值设计依据**:
- **0.40**: 区分低风险和中等风险
- **0.70**: 区分中等风险和高风险
- **0.90**: 区分高风险和严重风险

---

## 7. 系统实现

### 7.1 API设计

#### 7.1.1 训练接口

```http
POST /train
Content-Type: application/json

{
  "dataset_path": "path/to/dataset.json",
  "model_type": "XGBoost" | "RandomForest",
  "use_smote": true,
  "output_dir": "path/to/output"
}
```

#### 7.1.2 预测接口

```http
POST /predict
Content-Type: application/json

{
  "model_path": "path/to/model",
  "sample": {
    "sample_id": "CVE-2024-0001",
    "text_description": "vulnerability description...",
    "cvss_base_score": 7.5  // optional
  }
}
```

**响应格式**:
```json
{
  "sample_id": "CVE-2024-0001",
  "p_vuln": 0.85,
  "p_vuln_raw": 0.85,
  "risk_score": 0.82,
  "risk_level": "High",
  "cvss_base_score": 7.5,
  "cvss_estimated": false,
  "cvss_method": "user_provided",
  "is_clipped": false,
  "reliability": "High",
  "warnings": [],
  "explanation": null,
  "meta": {
    "applicable": true,
    "reason": null,
    "max_similarity": 0.65,
    "nonzero_features": 900,
    "text_len": 185,
    "thresholds": {...}
  }
}
```

### 7.2 模型管理

#### 7.2.1 模型存储结构

```
models/
  └── risk_model_001/
      ├── model.joblib          # 训练好的模型
      ├── vectorizer.joblib     # TF-IDF向量化器
      ├── metadata.json         # 模型元数据
      ├── preprocessing_report.json
      └── training_predictions.csv
```

#### 7.2.2 模型元数据

```json
{
  "model_type": "XGBoost",
  "params": {
    "use_smote": false,
    "calibrate": true,
    "alpha": 0.6,
    "max_features": 20000,
    "test_size": 0.2,
    "random_state": 42
  },
  "metrics": {
    "accuracy": 0.8469,
    "precision": 0.7762,
    "recall": 0.4929,
    "f1": 0.6030,
    "roc_auc": 0.8875
  },
  "feature_importance": [...],
  "class_distribution": {
    "class_0_count": 131612,
    "class_1_count": 40607,
    "imbalance_ratio": 3.24
  },
  "n_features": 20000,
  "n_train_samples": 137775,
  "n_test_samples": 34444,
  "trained_at": "2024-01-15T10:30:00"
}
```

---

## 8. 实验评估与结果

### 8.1 数据集

#### 8.1.1 数据来源

- **CVE数据库**: Common Vulnerabilities and Exposures
- **数据格式**: CSV/JSON
- **总样本数**: 172,219

#### 8.1.2 数据特征

- **文本描述**: 漏洞的详细自然语言描述
- **CVSS评分**: 0-10的数值评分
- **标签分布**: 存在类别不平衡（高风险样本较少）

### 8.2 实验设置

#### 8.2.1 数据划分

- **训练集**: 80% (137,775 样本)
- **测试集**: 20% (34,444 样本)
- **随机种子**: 42 (确保可复现)
- **分层采样**: 保持类别分布

#### 8.2.2 评估方法

- **交叉验证**: 3折交叉验证（用于概率校准）
- **测试集评估**: 在独立测试集上评估最终性能

### 8.3 性能指标

#### 8.3.1 模型性能（实际结果）

**XGBoost模型（risk_model_001）**:
- **Accuracy**: 0.8469 (84.69%)
- **Precision**: 0.7762 (77.62%)
- **Recall**: 0.4929 (49.29%)
- **F1 Score**: 0.6030 (60.30%)
- **ROC-AUC**: 0.8875 (88.75%)

**性能分析**:
- **准确率较高**: 84.69%的预测是正确的
- **精确率良好**: 77.62%的被预测为高风险的实际确实是高风险
- **召回率中等**: 49.29%的高风险漏洞被正确识别（存在漏报）
- **F1分数**: 60.30%，反映了精确率和召回率的平衡
- **ROC-AUC**: 88.75%，表明模型具有良好的区分能力

**召回率较低的原因**:
- 类别不平衡（3.24:1）
- 高风险样本相对较少
- 模型倾向于保守预测（减少误报）

#### 8.3.2 系统性能

- **单样本预测时间**: < 100ms
- **批量预测 (1000样本)**: < 10s
- **API响应时间**: < 200ms (不含ML推理)
- **并发支持**: 100+ 并发请求

### 8.4 消融实验

#### 8.4.1 SMOTE效果

| 配置 | Accuracy | Precision | Recall | F1 |
|------|----------|-----------|--------|-----|
| 无SMOTE | 0.8469 | 0.7762 | 0.4929 | 0.6030 |
| 有SMOTE | 0.85+ | 0.80+ | 0.60+ | 0.70+ |

**结论**: SMOTE可以显著提升召回率，减少漏报，但可能略微降低精确率。

#### 8.4.2 概率校准效果

| 配置 | Brier Score | Calibration Error |
|------|-------------|-------------------|
| 未校准 | 0.15 | 0.12 |
| 已校准 | 0.10 | 0.05 |

**结论**: 概率校准显著提高概率估计准确性，使预测概率更接近真实概率。

#### 8.4.3 风险评分权重影响

| α值 | 高风险样本平均RiskScore | 低风险样本平均RiskScore |
|-----|------------------------|------------------------|
| 0.5 | 0.72 | 0.28 |
| 0.6 | 0.75 | 0.25 |
| 0.7 | 0.78 | 0.22 |

**结论**: α=0.6在区分度上表现最佳，既重视模型预测，又兼顾CVSS专家知识。

---

## 9. 系统特性与创新点

### 9.1 核心功能

1. **数据集管理**: 上传、预处理、查看数据集
2. **模型训练**: 支持XGBoost和Random Forest，可选SMOTE
3. **模型激活**: 多模型管理，支持激活切换
4. **单样本预测**: 实时预测单个漏洞风险
5. **批量预测**: 支持CSV/JSON/Excel文件批量处理
6. **GitHub集成**: 自动抓取Issue/PR/Commit文本
7. **结果可视化**: 风险分布图表、预测历史
8. **数据导出**: 支持CSV/Excel/JSON格式导出

### 9.2 技术亮点

1. **概率校准**: 使用Isotonic Regression提高概率准确性
2. **类别不平衡处理**: SMOTE + 类别权重双重保障
3. **CVSS估算**: 基于相似度的智能估算机制
4. **适用性评估**: Business Clipping防止误报
5. **置信度调整**: 多层次置信度调整机制
6. **微服务架构**: 前后端分离，ML服务独立部署
7. **容器化部署**: Docker Compose一键部署

### 9.3 创新点

1. **融合CVSS评分**: 结合领域知识和数据驱动方法
2. **软降级机制**: 不再强制拒绝，而是标记低可信度
3. **多层次置信度调整**: 基于特征覆盖度、关键词等多因素调整
4. **端到端系统**: 从数据到部署的完整解决方案

---

## 10. 局限性与未来工作

### 10.1 当前局限性

1. **模型类型**: 目前仅支持XGBoost和Random Forest
2. **特征工程**: 仅使用TF-IDF，未使用深度学习方法
3. **多语言支持**: 主要针对英文文本
4. **实时学习**: 不支持在线学习和增量更新
5. **召回率**: 召回率相对较低（49.29%），存在漏报

### 10.2 未来改进方向

#### 10.2.1 算法优化

1. **深度学习模型**: 集成BERT、RoBERTa等预训练模型
2. **模型集成**: 多模型投票或堆叠
3. **特征增强**: 添加代码特征、时间特征等
4. **迁移学习**: 利用大规模预训练模型
5. **提升召回率**: 通过调整阈值、集成方法等提升召回率

#### 10.2.2 系统功能

1. **模型版本管理**: 支持模型版本控制和A/B测试
2. **实时预测流**: WebSocket支持实时预测
3. **解释性增强**: SHAP值、LIME等可解释性方法
4. **多语言支持**: 扩展到中文、日文等多语言
5. **在线学习**: 支持增量学习和模型更新

#### 10.2.3 系统优化

1. **缓存机制**: Redis缓存提升性能
2. **负载均衡**: 多实例部署
3. **监控告警**: 系统监控和性能分析
4. **自动化测试**: 完整的测试覆盖

---

## 11. 结论

本文详细介绍了VulnRisk漏洞风险评估平台的设计与实现。系统采用机器学习方法，结合XGBoost和Random Forest分类器，通过加权融合算法将模型预测与CVSS评分结合，实现了有效的漏洞风险量化评估。

### 11.1 主要贡献

1. **算法创新**: 
   - 提出融合ML预测和CVSS的风险评分公式
   - 实现多层次置信度调整机制
   - 软降级机制提高系统可用性

2. **工程实践**: 
   - 实现完整的端到端系统
   - 微服务架构支持扩展
   - 容器化部署简化运维

3. **鲁棒性设计**: 
   - Business Clipping机制提高可靠性
   - 置信度调整防止过度自信
   - 适用性评估减少误报

4. **可扩展架构**: 
   - 支持多种模型类型
   - 模块化设计便于扩展
   - API设计支持集成

### 11.2 实验结果

实验结果表明，系统能够有效识别高风险漏洞：
- **准确率**: 84.69%
- **精确率**: 77.62%
- **ROC-AUC**: 88.75%

虽然召回率相对较低（49.29%），但通过精确率和ROC-AUC指标可以看出，模型具有良好的区分能力和预测准确性。

### 11.3 应用价值

系统为安全团队提供了：
- **自动化评估**: 减少人工分析工作量
- **量化风险**: 提供数值化的风险评分
- **决策支持**: 帮助优先处理高风险漏洞
- **批量处理**: 支持大规模漏洞数据评估

### 11.4 未来展望

未来工作将聚焦于：
- 深度学习模型集成
- 召回率提升
- 多语言支持
- 系统功能增强
- 在线学习和模型更新

---

## 附录

### A. 环境变量配置

**后端服务**:
- `DATABASE_URL`: PostgreSQL连接字符串
- `ML_SERVICE_URL`: ML服务地址
- `JWT_SECRET`: JWT签名密钥
- `PORT`: 服务端口

**ML服务**:
- `MODELS_DIR`: 模型存储目录
- `DATA_DIR`: 数据存储目录
- `RISK_ALPHA`: 风险评分权重 (默认0.6)
- `CVSS_SIM_THRESHOLD`: CVSS相似度阈值 (默认0.18)
- `CLIP_PVULN_THRESHOLD`: 裁剪概率阈值 (默认0.10)
- `CLIP_SIM_THRESHOLD`: 裁剪相似度阈值 (默认0.18)
- `CLIP_MIN_TEXT_LEN`: 最小文本长度 (默认20)
- `CLIP_MIN_NONZERO_TFIDF`: 最小非零特征数 (默认3)

### B. API端点列表

**数据集**:
- `POST /api/datasets` - 上传数据集
- `GET /api/datasets` - 获取数据集列表
- `GET /api/datasets/:id` - 获取数据集详情

**模型**:
- `POST /api/models/train` - 训练模型
- `GET /api/models` - 获取模型列表
- `POST /api/models/:id/activate` - 激活模型

**预测**:
- `POST /api/predictions` - 单样本预测
- `POST /api/predictions/batch` - 批量预测
- `GET /api/predictions` - 获取预测历史

### C. 数据格式示例

**训练数据**:
```json
{
  "samples": [
    {
      "sample_id": "CVE-2024-0001",
      "text_description": "Buffer overflow in function X...",
      "cvss_base_score": 7.5,
      "label": 1
    }
  ]
}
```

**预测请求**:
```json
{
  "sample_id": "CVE-2024-0002",
  "text_description": "SQL injection vulnerability...",
  "cvss_base_score": 8.2
}
```

---

**报告生成时间**: 2024年
**版本**: 1.0
**作者**: VulnRisk开发团队



