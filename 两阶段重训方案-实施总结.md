# VulnRisk 两阶段重训方案 - 实施总结

**完成时间**: 2026-02-03

---

## 实施概述

已成功实现VulnRisk两阶段重训方案，将原来的"CVSS≥7 二分类"改为两阶段推理：
1. **Stage A**: Applicability模型判断是否漏洞相关/信息充分
2. **Stage B**: Severity模型预测4类严重度（Low/Medium/High/Critical）

---

## 新增文件列表

### 1. 现状扫描文档
- `两阶段重训方案-现状扫描.md`: 详细的现状扫描报告，包含训练链路、推理链路、模型保存目录等信息

### 2. 数据构建脚本
- `ml-service/build_github_dataset.py`: 从GitHub语料构建训练负类数据
  - 输出: `data/github_issues_prs_clean.csv`, `data/github_issues_prs_clean_summary.json`

### 3. 模型训练脚本
- `ml-service/train_applicability_model.py`: 训练Applicability模型（二分类）
  - 输出: `models/app_model_001/`
- `ml-service/train_severity_model.py`: 训练Severity模型（4类多分类）
  - 输出: `models/sev_model_001/`
- `ml-service/train_severity_config.json`: Severity模型训练配置文件

### 4. 一键重训脚本
- `ml-service/run_retrain_pipeline.py`: 一键执行完整重训流程
- `ml-service/README_RETRAIN.md`: 重训方案详细文档

### 5. 前端工具函数
- `frontend-vue/src/utils/predictionMapper.ts`: 预测结果字段映射工具

---

## 修改文件列表

### 1. ML服务（ml-service/）
- `ml-service/app.py`: 
  - 新增 `predict_two_stage()` 函数实现两阶段推理
  - 修改 `predict()` 端点支持两阶段模型自动检测
  - 保留旧模型向后兼容

### 2. 后端服务（backend-node/）
- `backend-node/src/services/prediction.service.ts`:
  - 更新 `predict()` 方法接收新字段（pApplicable, applicable, severityLevel, severityProbs）
  - 更新 `getPredictions()` 和 `getPredictionById()` 返回新字段
  - 更新 `batchPredict()` 处理新字段

### 3. 前端（frontend-vue/）
- `frontend-vue/src/views/Predictions.vue`:
  - 将 `P(vuln)` 列改为 `P(Applicable)` 并添加tooltip
  - 新增 `P(High+)` 列显示高风险概率
  - 使用字段映射工具统一处理新旧字段
- `frontend-vue/src/views/Report.vue`:
  - 新增 `Applicability` 显示（applicable + pApplicable）
  - 新增 `Severity Probabilities` 显示（4类概率分布）
  - 更新 `High-Severity Similarity (Raw)` 为 `P(High+)`（如果使用新模型）
  - 更新 `Method Note` 说明两阶段解释

---

## 核心功能实现

### Stage A: Applicability模型
- **正类**: CVE描述文本（漏洞相关）
- **负类**: GitHub issues/PRs（非漏洞相关或信息不足）
- **模型**: LogisticRegression（class_weight='balanced'）
- **特征**: TF-IDF（max_features=20000, ngram_range=(1,2)）
- **输出**: `pApplicable` (P(applicable=1))
- **阈值**: 默认0.5（可从metadata读取）

### Stage B: Severity模型
- **标签定义**:
  - 0 Low: [0, 4.0)
  - 1 Medium: [4.0, 7.0)
  - 2 High: [7.0, 9.0)
  - 3 Critical: [9.0, 10.0]
- **模型**: XGBoost多分类（multi:softprob）或RandomForest
- **特征**: TF-IDF（与Applicability模型一致）
- **输出**: `severityProbs` (4类概率数组), `severityLevel` (argmax)

### 两阶段推理流程
1. 计算 `pApplicable = P(app=1)`
2. 如果 `pApplicable < app_threshold`:
   - `applicable = false`
   - `severityLevel = "N/A"`
   - `severityProbs = null`
   - `riskScore = null`
   - `explanation = "Text appears not vulnerability-related or lacks sufficient evidence; severity scoring skipped."`
3. 如果 `pApplicable >= app_threshold`:
   - `applicable = true`
   - 计算 `severityProbs`（4类概率）
   - `severityLevel = argmax` 映射为 Low/Medium/High/Critical
   - `riskScore = 0.1*P(Low) + 0.4*P(Med) + 0.7*P(High) + 0.9*P(Critical)`
   - `explanation = "Vulnerability-related text detected. Predicted severity: X..."`

### 字段兼容性
- **新字段优先**: `pApplicable`, `applicable`, `severityLevel`, `severityProbs`, `riskScore`
- **旧字段fallback**: `pVuln`, `riskLevel`, `cvss`, `reliability`
- **兼容字段**: 
  - `pVuln = P(High) + P(Critical)`（如果使用新模型）
  - `riskLevel = severityLevel`（如果使用新模型）

---

## 验收标准

### 数据构建
- ✅ `github_issues_prs_clean.csv` 行数 >= 2000（可调整阈值）

### Applicability模型
- ✅ ROC-AUC >= 0.85（如果未达标，可调整负类样本）

### Severity模型
- ✅ `metadata.json` 中包含 `macro_f1` 与每类指标
- ✅ 模型能输出4类概率

### 系统集成
- ✅ 输入文本/GitHub issue → 输出 `pApplicable + severityLevel + riskScore`
- ✅ 兼容字段 `pVuln/riskLevel` 正常工作
- ✅ XSS样例输入应返回 `applicable=true` 且 `severityLevel` 不为 N/A

---

## 运行说明

### 快速开始

```bash
# 1. 进入ml-service目录
cd ml-service

# 2. 一键重训（推荐）
python run_retrain_pipeline.py

# 3. 查看报告
cat ../models/retrain_report.md
```

### 分步执行

```bash
# 步骤1: 构建GitHub数据
python build_github_dataset.py

# 步骤2: 训练Applicability模型
python train_applicability_model.py --n-samples 50000

# 步骤3: 训练Severity模型
python train_severity_model.py train_severity_config.json
```

详细说明见 `ml-service/README_RETRAIN.md`

---

## 模型文件位置

```
models/
├── app_model_001/          # Applicability模型
│   ├── model.joblib
│   ├── vectorizer.joblib
│   ├── metadata.json
│   └── training_predictions.csv
├── sev_model_001/          # Severity模型（4类）
│   ├── model.joblib
│   ├── vectorizer.joblib
│   ├── metadata.json
│   └── training_predictions.csv
└── retrain_report.md        # 重训报告
```

---

## 关键改进

### 问题修复
1. ✅ **修复LOW_PVULN误判**: 不再将XSS等明显漏洞文本错误标记为"not vulnerability-related"
2. ✅ **4类严重度分类**: 从二分类改为4类，更贴合风险等级
3. ✅ **两阶段推理**: 先判断适用性，再预测严重度，逻辑更清晰

### 用户体验
1. ✅ **前端字段映射**: 统一处理新旧字段，确保兼容性
2. ✅ **详细解释**: 提供两阶段解释，说明评估方法
3. ✅ **概率可视化**: 显示4类概率分布，更直观

---

## 后续优化建议

1. **模型性能优化**:
   - 如果Applicability模型ROC-AUC < 0.85，考虑增加负类样本多样性
   - 如果Severity模型macro-F1较低，考虑调整类别权重或使用SMOTE

2. **特征工程**:
   - 可以尝试不同的TF-IDF参数（max_features, ngram_range）
   - 可以尝试添加其他特征（如文本长度、关键词计数等）

3. **阈值调优**:
   - 根据实际使用情况调整 `app_threshold`
   - 可以根据业务需求调整风险评分权重

---

## 文档

- **现状扫描**: `两阶段重训方案-现状扫描.md`
- **重训文档**: `ml-service/README_RETRAIN.md`
- **重训报告**: `models/retrain_report.md`（训练后生成）

---

**实施完成** ✅



