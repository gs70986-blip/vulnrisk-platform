# 前端重构总结：对齐风险评估语义和论文演示清晰度

## 修改的文件

### 1. `frontend-vue/src/views/Predictions.vue`

#### 删除的列：
- **P(High+)** 列（第69-90行）- 已删除
- **CVSS** 列（第131-137行）- 已删除

#### 修改的列：
- **P(Applicable)** → **Applicability**（重命名，保持功能）
- **Risk Score** → **Risk Score (Reference)**（重命名）

#### 保留的列：
- Applicability（适用性）
- Risk Score (Reference)（风险评分参考值）
- Risk Level（风险等级）
- Created At（创建时间）
- Actions（操作）

### 2. `frontend-vue/src/views/Report.vue`

#### 删除的字段：
- **pVuln** 显示 - 已删除（不再显示）
- **CVSS Base Score** 字段（第96-102行）- 已删除
- **P(High+)** 字段（第71-87行）- 已删除

#### 新增的可视化：
- **Severity Probability Distribution**（严重度概率分布）
  - 使用 ECharts 柱状图
  - 显示 Low/Medium/High/Critical 四个级别的概率
  - 每个柱子使用对应颜色（绿色/黄色/橙色/红色）
  - 显示百分比标签

#### 字段显示顺序（已调整）：
1. Sample ID
2. Applicability（适用性）
3. Severity Level（严重度等级）
4. Risk Score (Reference)（风险评分参考值）
5. Risk Level（风险等级）
6. Reliability（可靠性）
7. Model（模型信息）
8. Created At（创建时间）

#### 重命名的部分：
- **Evidence in Text** → **Keyword Indicators (Heuristic)**
  - 添加副标题："Extracted via rule-based matching, not a model explanation"

#### 更新的说明文本：
- Method Note 部分更新：
  - 明确说明风险评估是条件性的（conditional on applicability）
  - 强调严重度概率分布用于优先级排序，不代表漏洞存在
  - 明确说明系统不计算官方 CVSS 分数

## 语义检查

### ✅ 已确保：
1. **不暗示系统计算官方 CVSS 分数**
   - 删除了 CVSS Base Score 字段显示
   - Method Note 中明确说明："This system does not compute official CVSS scores"

2. **不暗示 pVuln 是漏洞存在概率**
   - 删除了 pVuln 字段显示
   - 删除了 P(High+) 字段显示

3. **严重度分布可视化证明严重度等级和风险评分**
   - 添加了柱状图可视化
   - 图表清晰显示每个严重度级别的概率
   - 图表说明文本解释了分布的含义

4. **明确说明风险评估是条件性的**
   - Method Note 中明确说明："The risk assessment is conditional on applicability"
   - 强调风险评分是参考估计值（reference estimate）

## 技术实现

### 使用的库：
- **ECharts** + **vue-echarts**：用于严重度概率分布可视化
- 已集成到项目中（参考 `Models.vue` 的实现）

### 图表配置：
- 类型：柱状图（Bar Chart）
- X 轴：严重度级别（Low, Medium, High, Critical）
- Y 轴：概率（0-1，显示为百分比）
- 颜色：绿色（Low）、黄色（Medium）、橙色（High）、红色（Critical）
- 标签：显示百分比数值

## 确认

### ✅ 未修改：
- 后端 API 接口
- 模型逻辑
- 数据库结构
- 预测服务逻辑

### ✅ 仅修改：
- 前端显示逻辑
- 前端组件结构
- 前端文本和说明

## 验收标准

1. ✅ Predictions 页面不再显示 P(High+) 和 CVSS 列
2. ✅ Predictions 页面 Risk Score 显示为 "Risk Score (Reference)"
3. ✅ Report 页面不再显示 pVuln 和 CVSS Base Score
4. ✅ Report 页面显示严重度概率分布柱状图
5. ✅ Report 页面字段按正确顺序显示
6. ✅ Evidence section 重命名为 "Keyword Indicators (Heuristic)"
7. ✅ 所有说明文本不提及 pVuln 或 CVSS
8. ✅ 明确说明风险评估是条件性的
9. ✅ 强调优先级排序和参考用途


