# VulnRisk 两阶段重训方案 - 现状扫描

**生成时间**: 2026-02-03

---

## A. 现状扫描（定位清单）

### 1. 关键组件定位

#### ml-service/ 关键组件

| 关键词 | 文件路径 | 关键函数/类 | 说明 |
|--------|----------|------------|------|
| **TfidfVectorizer** | `ml-service/train_risk_model.py:15, 61` | `TfidfVectorizer()` | TF-IDF向量化器，max_features=20000, ngram_range=(1,2) |
| **SMOTE** | `ml-service/train_risk_model.py:21, 285-286` | `SMOTE()`, `fit_resample()` | 类别不平衡处理，可选使用 |
| **CalibratedClassifierCV** | `ml-service/train_risk_model.py:16, 126` | `CalibratedClassifierCV()` | 概率校准，使用Isotonic回归，3折交叉验证 |
| **clip_** | `ml-service/app.py:38-42, 336-340` | `CLIP_*` 环境变量, `assess_applicability()` | 工程裁剪机制，防止误报 |
| **similarity** | `ml-service/risk.py:62-163` | `estimate_cvss_from_similarity()` | 基于余弦相似度估算CVSS评分 |
| **p_vuln** | `ml-service/app.py:245, 261-291, 411-412`<br>`ml-service/risk.py:10, 18, 26` | `p_vuln`, `p_vuln_raw`, `calculate_risk_score()` | 漏洞概率，核心预测变量 |
| **risk_level** | `ml-service/risk.py:42-59`<br>`ml-service/app.py:383-393, 414` | `get_risk_level()` | 风险等级映射函数 |

#### backend-node/ 关键组件

| 关键词 | 文件路径 | 关键函数/路由 | 说明 |
|--------|----------|--------------|------|
| **/api/models/train** | `backend-node/src/routes/models.ts:11` | `POST /api/models/train` | 模型训练API端点 |
| **/api/predictions** | `backend-node/src/routes/predictions.ts:55-60` | `POST /api/predictions`<br>`POST /api/predictions/batch`<br>`GET /api/predictions` | 预测相关API端点 |
| **ML_SERVICE_URL** | `backend-node/src/config/index.ts:8`<br>`backend-node/src/services/model.service.ts:39` | `config.mlServiceUrl` | ML服务URL配置，默认http://localhost:5000 |

#### frontend-vue/ 关键组件

| 关键词 | 文件路径 | 关键变量/组件 | 说明 |
|--------|----------|--------------|------|
| **pVuln** | `frontend-vue/src/views/Report.vue:41-45, 262-264`<br>`frontend-vue/src/views/Predictions.vue:52-56`<br>`frontend-vue/src/services/api.ts:78-79` | `effectivePVulnRaw`, `prediction.pVuln` | 前端展示的漏洞概率 |
| **riskLevel** | `frontend-vue/src/views/Report.vue:80-81, 283-285`<br>`frontend-vue/src/views/Predictions.vue:70-79` | `effectiveRiskLevel`, `getRiskTagType()` | 风险等级展示和排序 |
| **Report.vue** | `frontend-vue/src/views/Report.vue` | 完整报告页面组件 | 单个预测结果详细报告 |
| **Predictions.vue** | `frontend-vue/src/views/Predictions.vue` | 预测列表页面组件 | 预测结果管理页面 |

---

### 2. 训练链路（路由→训练函数→保存模型）

```
前端/API请求
    ↓
POST /api/models/train
    ↓
backend-node/src/routes/models.ts:11
    ↓
backend-node/src/controllers/model.controller.ts:6 (trainModel)
    ↓
backend-node/src/services/model.service.ts:16 (trainModel)
    ↓
HTTP POST → ML_SERVICE_URL/train (http://localhost:5000/train)
    ↓
ml-service/app.py (Flask /train 端点，如果存在)
    ↓
ml-service/train_risk_model.py:main()
    ↓
ml-service/train_risk_model.py:build_feature_pipeline() → TfidfVectorizer
    ↓
ml-service/train_risk_model.py:train_model() → XGBoost/RandomForest + CalibratedClassifierCV
    ↓
ml-service/train_risk_model.py:train_model() → SMOTE (可选)
    ↓
保存模型到: models/risk_model_xxx/
    ├── model.joblib
    ├── vectorizer.joblib
    └── metadata.json
```

**注意**: 当前系统可能通过直接调用 `train_risk_model.py` 脚本训练，而非通过API。需要确认是否有 `/train` Flask端点。

---

### 3. 推理链路（路由→predict→后处理裁剪→返回字段）

```
前端/API请求
    ↓
POST /api/predictions
    ↓
backend-node/src/routes/predictions.ts:55
    ↓
backend-node/src/controllers/prediction.controller.ts (predict)
    ↓
backend-node/src/services/prediction.service.ts (predict)
    ↓
HTTP POST → ML_SERVICE_URL/predict (http://localhost:5000/predict)
    ↓
ml-service/app.py:186 (predict 端点)
    ↓
ml-service/app.py:217 (load_model_artifacts)
    ↓
ml-service/app.py:229 (preprocess_text_for_prediction)
    ↓
ml-service/app.py:232 (vectorizer.transform)
    ↓
ml-service/app.py:245 (model.predict_proba → p_vuln_raw)
    ↓
ml-service/app.py:261-291 (置信度调整 → p_vuln)
    ↓
ml-service/app.py:304-327 (estimate_cvss_from_similarity)
    ↓
ml-service/app.py:328-341 (assess_applicability → 工程裁剪)
    ↓
ml-service/app.py:350 (calculate_risk_score)
    ↓
ml-service/app.py:393 (get_risk_level)
    ↓
ml-service/app.py:409-437 (构建返回结果)
    ↓
返回字段:
{
    p_vuln, p_vuln_raw, risk_score, risk_level,
    cvss_base_score, cvss_estimated, cvss_method,
    is_clipped, reliability, warnings, explanation,
    meta: {applicable, reason, max_similarity, ...}
}
    ↓
backend-node/src/services/prediction.service.ts (存储到数据库)
    ↓
返回给前端
```

---

### 4. 模型保存目录与命名规则

**当前模型目录结构**:
```
models/
├── risk_model_001/          # XGBoost模型（无SMOTE）
│   ├── model.joblib         # 训练好的模型
│   ├── vectorizer.joblib    # TF-IDF向量化器
│   ├── metadata.json        # 模型元数据
│   ├── preprocessing_report.json
│   └── training_predictions.csv
├── risk_model_002_smote/    # XGBoost模型（使用SMOTE）
│   └── [同上结构]
└── risk_model_003_rf/       # RandomForest模型
    └── [同上结构]
```

**命名规则**:
- 目录名: `risk_model_xxx` 或 `risk_model_xxx_smote` 或 `risk_model_xxx_rf`
- 模型文件: `model.joblib`
- 向量化器: `vectorizer.joblib`
- 元数据: `metadata.json`

**新模型命名建议**:
- Applicability模型: `models/app_model_001/`
- Severity模型: `models/sev_model_001/`

---

### 5. 数据文件格式确认

#### GitHub语料格式 (data/github_corpus_issues_prs.jsonl)
```json
{
  "id": "repos/owner#number",
  "type": "issue" | "pr",
  "title": "...",
  "body": "...",
  "comments": [
    {"body": "...", "author": "...", "created_at": "..."}
  ],
  "url": "https://github.com/...",
  "owner": "...",
  "repo": "...",
  "number": 123,
  "state": "open" | "closed",
  "created_at": "...",
  "updated_at": "...",
  "search_query": "..."
}
```

**注意**: 没有 `text` 字段，需要从 `title`, `body`, `comments` 拼接。

#### CVE数据格式 (ml-service/merged_json_table.csv)
```csv
cve_id,description_clean,cvss_base,cvss_severity,...
```

**字段说明**:
- `description_clean`: 漏洞描述文本（用于训练）
- `cvss_base`: CVSS基础评分（0-10，用于构建标签）
- `cvss_severity`: 严重程度（LOW/MEDIUM/HIGH/CRITICAL）

---

## B. 训练链路详细分析

### 当前训练流程

1. **数据加载**: `data_exploration.py:load_cve_dataset()`
2. **数据预处理**: `data_exploration.py:preprocess_dataset()`
3. **特征工程**: `train_risk_model.py:build_feature_pipeline()` → TF-IDF
4. **数据划分**: `train_test_split()` (80/20)
5. **SMOTE处理**: 可选，`SMOTE.fit_resample()`
6. **模型训练**: `train_risk_model.py:train_model()` → XGBoost/RandomForest
7. **概率校准**: `CalibratedClassifierCV()` (可选)
8. **模型评估**: 计算accuracy, precision, recall, f1, roc_auc
9. **模型保存**: `joblib.dump()` 保存到 `models/risk_model_xxx/`

---

## C. 推理链路详细分析

### 当前预测流程

1. **文本预处理**: `app.py:preprocess_text_for_prediction()` → 小写、去特殊字符
2. **特征提取**: `vectorizer.transform()` → TF-IDF向量
3. **模型预测**: `model.predict_proba()` → `p_vuln_raw`
4. **置信度调整**: 基于特征覆盖度、关键词等调整 `p_vuln`
5. **CVSS估算**: `estimate_cvss_from_similarity()` 或 `estimate_cvss_from_p_vuln()`
6. **适用性评估**: `assess_applicability()` → 工程裁剪检查
7. **风险评分**: `calculate_risk_score(p_vuln, cvss_base_score)`
8. **风险等级**: `get_risk_level(risk_score)` → Low/Medium/High/Critical
9. **返回结果**: 包含所有字段的JSON响应

---

## D. 问题识别

### 当前问题

1. **LOW_PVULN 直接判 N/A**: 
   - 位置: `ml-service/app.py:368, 378`
   - 问题: 当 `p_vuln < 0.10` 时，直接标记为"not vulnerability-related"，这是错误的
   - 影响: XSS等明显漏洞文本被错误拒绝

2. **二分类限制**:
   - 当前: CVSS≥7.0 → 高风险，CVSS<7.0 → 低风险
   - 问题: 无法区分4个风险等级，XSS可能被误判为低风险

3. **缺少Applicability模型**:
   - 当前: 使用硬编码规则判断适用性
   - 问题: 规则不够准确，容易误判

---

## E. 两阶段方案设计

### Stage A: Applicability 模型
- **目标**: 判断输入是否"漏洞相关/信息充分"
- **正类**: CVE描述文本（漏洞相关）
- **负类**: GitHub issues/PRs（非漏洞相关或信息不足）
- **输出**: `pApplicable` (P(applicable=1))

### Stage B: Severity 模型
- **目标**: 预测漏洞严重度（4类）
- **标签**: Low(0-4), Medium(4-7), High(7-9), Critical(9-10)
- **输出**: `severityProbs` (4类概率数组)

### 推理流程
1. 先计算 `pApplicable`
2. 如果 `pApplicable < threshold` → 返回 N/A
3. 否则计算 `severityProbs` → 映射到 `severityLevel`
4. 计算 `riskScore` = 期望加权

---

**扫描完成时间**: 2026-02-03



