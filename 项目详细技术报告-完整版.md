# VulnRisk 漏洞风险评估平台 - 详细技术报告

**版本**: 2.0.0  
**日期**: 2026年2月4日  
**项目类型**: 基于机器学习的软件漏洞风险评估系统

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [数据准备与处理](#3-数据准备与处理)
4. [模型训练详细说明](#4-模型训练详细说明)
5. [风险评估机制](#5-风险评估机制)
6. [系统集成与部署](#6-系统集成与部署)
7. [性能评估与实验结果](#7-性能评估与实验结果)
8. [技术栈与依赖](#8-技术栈与依赖)
9. [未来改进方向](#9-未来改进方向)

---

## 1. 项目概述

### 1.1 项目背景

随着软件系统的复杂性和规模不断增长，软件漏洞已成为网络安全的主要威胁之一。传统的漏洞检测方法主要依赖人工审计和静态分析工具，效率低下且难以应对大规模代码库。本项目旨在构建一个基于机器学习的自动化漏洞风险评估平台，通过分析漏洞描述文本和CVSS评分，自动预测漏洞风险等级，为安全团队提供决策支持。

### 1.2 核心功能

1. **两阶段漏洞风险评估**
   - Stage A: 适用性判断（Applicability Model）- 判断输入文本是否与漏洞相关
   - Stage B: 严重度预测（Severity Model）- 预测漏洞的严重程度等级

2. **多模型支持**
   - 支持 LogisticRegression、XGBoost、RandomForest 三种算法
   - 模型注册与激活机制
   - 模型版本管理

3. **综合风险评分**
   - 结合ML预测概率和CVSS评分
   - 输出风险等级（Low/Medium/High/Critical）
   - 提供详细的预测解释

4. **完整的Web平台**
   - 数据集管理
   - 模型训练与评估
   - 批量/单个预测
   - 风险分析与可视化

### 1.3 系统定位

- **学术原型**: 完整的机器学习管道和评估指标
- **工程可运行**: 生产级代码质量和系统架构
- **论文复现**: 支持实验复现和结果验证
- **系统演示**: 可部署演示系统

---

## 2. 系统架构

### 2.1 整体架构

VulnRisk采用微服务架构，将系统分为三个主要服务：

```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│  Vue 3 前端      │ ───→ │  Node.js 后端    │ ───→ │  Python ML 服务  │
│  (Element Plus)  │      │  (Express)       │      │  (Flask)         │
└─────────────────┘      └─────────────────┘      └─────────────────┘
                                │
                                ↓
                         ┌─────────────────┐
                         │   PostgreSQL    │
                         │     数据库      │
                         └─────────────────┘
```

### 2.2 技术栈

**前端层**
- Vue 3 + TypeScript
- Element Plus UI组件库
- ECharts 数据可视化
- Pinia 状态管理
- Vite 构建工具

**后端层**
- Node.js 18+ + TypeScript
- Express.js Web框架
- Prisma ORM 数据库访问
- JWT 认证授权
- PostgreSQL 数据库

**机器学习服务**
- Python 3.10+
- Flask RESTful API
- scikit-learn 机器学习库
- XGBoost 梯度提升算法
- imbalanced-learn 不平衡数据处理
- joblib 模型持久化

### 2.3 数据流向

```
用户输入文本
    ↓
前端界面 (Vue)
    ↓
后端API (Node.js)
    ↓
ML服务 (Python Flask)
    ↓
Stage A: Applicability模型
    ├─ 判断是否漏洞相关
    └─ 输出 pApplicable
    ↓
Stage B: Severity模型 (如果 applicable=true)
    ├─ 预测4类严重度概率
    └─ 输出 severityProbs [P(Low), P(Medium), P(High), P(Critical)]
    ↓
风险评分计算
    ├─ 计算 riskScore
    └─ 映射到风险等级
    ↓
返回结果到前端
    ↓
可视化展示
```

---

## 3. 数据准备与处理

### 3.1 正类数据（CVE漏洞描述）

**数据源**: `ml-service/merged_json_table.csv`

**字段**:
- `description_clean`: 清洗后的CVE漏洞描述文本
- `cvss_base`: CVSS基础评分（0-10）
- `cvss_severity`: CVSS严重程度（Low/Medium/High/Critical）

**数据预处理**:
- 过滤空值和无效文本
- 文本长度要求: `len(description_clean) >= 20`
- 文本清洗: 转小写、移除特殊字符、标准化空白字符
- 采样策略: 固定随机种子（SEED=42），最大采样数可配置（默认80000）

**数据统计**:
- 总样本数: 约50,000+ CVE记录
- 文本平均长度: 约200-500字符
- CVSS评分分布: 覆盖0-10全范围

### 3.2 负类数据（GitHub Issues/PRs）

**数据源**: `ml-service/negative_github_issues.csv`

**数据来源**:
- 从GitHub API抓取的普通工程issue和PR
- 排除安全相关关键词（vulnerability, CVE, XSS, SQLi等）
- 涵盖多种工程场景：bug修复、性能优化、UI改进、文档更新等

**数据预处理**:
- 过滤空值和无效文本
- 文本长度要求: `len(text) >= 60`
- 本地安全过滤: 使用正则表达式进一步排除安全相关文本
- 采样策略: 最大采样数可配置（默认10000），不足则全量使用

**数据统计**:
- 总样本数: 约49,000+ GitHub记录
- 文本平均长度: 约100-300字符
- 类别分布: 负类与正类基本平衡（约1:1）

### 3.3 特征工程

**TF-IDF向量化**:
- `max_features`: 20000（最大特征数）
- `ngram_range`: (1, 2)（1-gram和2-gram）
- `stop_words`: 'english'（移除英文停用词）
- `min_df`: 2（最小文档频率）
- `max_df`: 0.95（最大文档频率，过滤过于常见的词）

**文本预处理流程**:
```python
1. 转小写: text.lower()
2. 移除特殊字符: re.sub(r'[^a-z0-9\s]', ' ', text)
3. 标准化空白: ' '.join(text.split())
```

---

## 4. 模型训练详细说明

### 4.1 Stage A: Applicability模型（适用性判断）

#### 4.1.1 任务定义

**目标**: 判断输入文本是否与漏洞相关或包含足够信息进行风险评估

**标签定义**:
- `label=1` (正类): CVE漏洞描述文本（漏洞相关）
- `label=0` (负类): GitHub issues/PRs（非漏洞相关或信息不足）

**输出**:
- `pApplicable`: P(applicable=1) 概率值 [0, 1]
- `applicable`: 布尔值，基于阈值判断（默认阈值0.5）

#### 4.1.2 支持的模型算法

**1. LogisticRegression（基线模型）**

**配置参数**:
```python
LogisticRegression(
    max_iter=4000,           # 最大迭代次数
    class_weight='balanced',  # 自动平衡类别权重
    C=0.5,                   # 正则化强度
    n_jobs=-1,               # 并行计算
    random_state=42          # 随机种子
)
```

**特点**:
- 作为baseline对照模型
- 训练速度快，可解释性强
- 适合快速验证和对比

**2. XGBoost（推荐模型）**

**配置参数**:
```python
XGBClassifier(
    objective='binary:logistic',  # 二分类目标函数
    n_estimators=600,             # 树的数量
    max_depth=6,                  # 树的最大深度
    learning_rate=0.05,           # 学习率
    subsample=0.8,                # 样本采样比例
    colsample_bytree=0.8,         # 特征采样比例
    reg_lambda=1.0,                # L2正则化
    scale_pos_weight=neg_count/pos_count,  # 类别权重（自动计算）
    random_state=42,
    eval_metric='logloss',
    use_label_encoder=False
)
```

**特点**:
- 性能优秀，适合作为论文主模型
- 支持特征重要性分析
- 对类别不平衡有良好处理

**3. RandomForest（备选模型）**

**配置参数**:
```python
RandomForestClassifier(
    n_estimators=800,                    # 树的数量
    class_weight='balanced_subsample',    # 平衡子样本类别权重
    random_state=42,
    n_jobs=-1                            # 并行计算
)
```

**特点**:
- 集成学习，稳定性好
- 对过拟合有较强抵抗力
- 适合作为对比模型

#### 4.1.3 训练流程

**数据划分**:
```python
train_test_split(
    X, y,
    test_size=0.2,           # 测试集比例20%
    random_state=42,         # 固定随机种子
    stratify=y               # 分层采样保持类别分布
)
```

**评估指标**:
- **ROC-AUC**: 接收者操作特征曲线下面积（主要指标）
- **PR-AUC**: 精确率-召回率曲线下面积
- **Accuracy**: 准确率
- **Precision**: 精确率
- **Recall**: 召回率
- **F1-Score**: F1分数

**阈值优化**:
- 默认阈值: `app_threshold=0.5`
- 可选阈值扫描: 0.3-0.8，步长0.05
- 选择F1分数最高的阈值

#### 4.1.4 模型保存结构

每个模型保存在独立目录下：

```
models/app_model_001_{lr|xgb|rf}/
├── model.joblib              # 训练好的模型
├── vectorizer.joblib         # TF-IDF向量化器
├── metadata.json             # 模型元数据
└── eval_predictions.csv      # 测试集预测结果
```

**metadata.json 包含**:
- 模型类型和算法
- 训练参数配置
- 性能指标（ROC-AUC, PR-AUC等）
- 类别分布统计
- 数据源信息
- 训练时间戳

#### 4.1.5 实际训练结果

**XGBoost模型** (`app_model_001_xgb`):
- **ROC-AUC**: 0.9992
- **PR-AUC**: 0.9993
- **Accuracy**: 0.9889
- **Precision**: 0.9971
- **Recall**: 0.9808
- **F1-Score**: 0.9889
- **训练样本数**: 79,388
- **测试样本数**: 19,847
- **类别分布**: 正类50,000 (50.4%), 负类49,235 (49.6%)
- **最优阈值**: 0.3

**RandomForest模型** (`app_model_001_rf`):
- **ROC-AUC**: 0.9993
- **PR-AUC**: 0.9994
- **Accuracy**: 0.9921
- **Precision**: 0.9954
- **Recall**: 0.9889
- **F1-Score**: 0.9921
- **训练样本数**: 79,388
- **测试样本数**: 19,847
- **类别分布**: 正类50,000 (50.4%), 负类49,235 (49.6%)
- **最优阈值**: 0.5

**分析**:
- 两个模型都达到了极高的性能（ROC-AUC > 0.99）
- 说明正类和负类数据区分度很好
- RandomForest略优于XGBoost，但差异很小
- 两个模型都适合生产使用

### 4.2 Stage B: Severity模型（严重度预测）

#### 4.2.1 任务定义

**目标**: 预测漏洞的严重程度等级（4类多分类）

**标签定义**:
- `label=0` (Low): CVSS评分 [0, 4.0)
- `label=1` (Medium): CVSS评分 [4.0, 7.0)
- `label=2` (High): CVSS评分 [7.0, 9.0)
- `label=3` (Critical): CVSS评分 [9.0, 10.0]

**输出**:
- `severityProbs`: 4类概率数组 [P(Low), P(Medium), P(High), P(Critical)]
- `severityLevel`: 预测的严重度等级（argmax）

#### 4.2.2 模型配置

**XGBoost多分类**:
```python
XGBClassifier(
    objective='multi:softprob',  # 多分类目标函数
    num_class=4,                 # 类别数
    n_estimators=100,
    max_depth=6,
    learning_rate=0.1,
    random_state=42,
    eval_metric='mlogloss',      # 多分类对数损失
    use_label_encoder=False
)
```

**RandomForest多分类**:
```python
RandomForestClassifier(
    n_estimators=100,
    max_depth=None,
    class_weight='balanced',     # 自动平衡类别权重
    random_state=42,
    n_jobs=-1
)
```

#### 4.2.3 训练流程

**数据预处理**:
- 从CVE数据构建4类标签
- 优先使用`cvss_base`评分
- 如果`cvss_base`缺失，使用`cvss_severity`字符串映射
- 过滤无效标签（返回-1的样本）

**数据划分**:
- 测试集比例: 20%
- 分层采样: `stratify=y`
- 随机种子: 42

**评估指标**:
- **Accuracy**: 整体准确率
- **Macro Precision**: 宏平均精确率
- **Macro Recall**: 宏平均召回率
- **Macro F1**: 宏平均F1分数（主要指标）
- **Weighted F1**: 加权F1分数
- **Per-class Metrics**: 每类的精确率、召回率、F1分数

#### 4.2.4 实际训练结果

**XGBoost模型** (`sev_model_001`):
- **Accuracy**: 0.7185
- **Macro Precision**: 0.7603
- **Macro Recall**: 0.5328
- **Macro F1**: 0.5856
- **Weighted F1**: 0.7040

**每类性能**:
- **Low** (label=0):
  - Precision: 0.8567
  - Recall: 0.2747
  - F1: 0.4160
  - 样本数: 5,113 (5.4%)

- **Medium** (label=1):
  - Precision: 0.7360
  - Recall: 0.8552
  - F1: 0.7912
  - 样本数: 48,976 (51.8%)

- **High** (label=2):
  - Precision: 0.6761
  - Recall: 0.6731
  - F1: 0.6746
  - 样本数: 33,111 (35.0%)

- **Critical** (label=3):
  - Precision: 0.7724
  - Recall: 0.3282
  - F1: 0.4607
  - 样本数: 7,496 (7.9%)

**分析**:
- 类别不平衡问题明显：Medium类占51.8%，Low和Critical类较少
- Medium类表现最好（F1=0.79），说明中等严重度漏洞特征明显
- Low和Critical类召回率较低（0.27和0.33），存在欠拟合
- 整体Macro F1为0.59，仍有改进空间
- 可以考虑使用SMOTE或调整类别权重来改善少数类性能

**训练样本统计**:
- 训练集: 75,756样本
- 测试集: 18,940样本
- 总特征数: 20,000

### 4.3 模型注册与激活机制

#### 4.3.1 模型注册

**注册流程**:
1. 训练完成后，自动调用`register_model_to_db()`函数
2. 读取模型目录下的`metadata.json`
3. 调用Node.js脚本`backend-node/scripts/register-model.js`
4. 将模型信息写入PostgreSQL数据库

**注册信息**:
```json
{
  "modelId": "app_model_001_xgb",
  "type": "applicability",
  "path": "models/app_model_001_xgb",
  "createdAt": "2026-02-04T18:00:13.862923",
  "metrics": {
    "roc_auc": 0.9992,
    "pr_auc": 0.9993,
    "accuracy": 0.9889
  }
}
```

#### 4.3.2 模型激活

**激活机制**:
- 系统支持多个模型注册，但**只能有一个模型处于激活状态**
- 激活操作通过前端界面或API完成
- 激活的模型用于生产环境预测

**数据库字段**:
- `isActive`: 布尔值，标记模型是否激活
- 激活新模型时，自动将旧模型设为非激活状态

---

## 5. 风险评估机制

### 5.1 两阶段推理流程

#### 5.1.1 Stage A: 适用性判断

**输入**: 原始文本描述

**处理流程**:
1. 文本预处理（转小写、移除特殊字符、标准化空白）
2. TF-IDF向量化（使用Applicability模型的vectorizer）
3. 模型预测得到`pApplicable`
4. 阈值判断: `applicable = pApplicable >= app_threshold`

**输出**:
- 如果`applicable = false`:
  - `severityLevel = "N/A"`
  - `severityProbs = null`
  - `riskScore = null`
  - `explanation = "Text appears not vulnerability-related or lacks sufficient evidence; severity scoring skipped."`

- 如果`applicable = true`:
  - 继续执行Stage B

#### 5.1.2 Stage B: 严重度预测

**输入**: 预处理后的文本（applicable=true时）

**处理流程**:
1. TF-IDF向量化（使用Severity模型的vectorizer）
2. 模型预测得到4类概率: `[P(Low), P(Medium), P(High), P(Critical)]`
3. 选择概率最大的类别作为`severityLevel`
4. 计算风险评分

**风险评分公式**:
```python
riskScore = (
    0.1 * P(Low) +        # Low权重0.1
    0.4 * P(Medium) +     # Medium权重0.4
    0.7 * P(High) +       # High权重0.7
    0.9 * P(Critical)     # Critical权重0.9
)
```

**风险等级映射**:
```python
if riskScore < 0.4:
    riskLevel = "Low"
elif riskScore < 0.6:
    riskLevel = "Medium"
elif riskScore < 0.8:
    riskLevel = "High"
else:
    riskLevel = "Critical"
```

**输出**:
- `severityLevel`: "Low" | "Medium" | "High" | "Critical"
- `severityProbs`: `{Low: float, Medium: float, High: float, Critical: float}`
- `riskScore`: [0, 1] 浮点数
- `explanation`: 详细的预测解释

### 5.2 风险评分计算模块

#### 5.2.1 核心函数

**`calculate_risk_score(p_vuln, cvss_base_score, alpha=0.6)`**

**公式**:
```
RiskScore = α * P(vuln) + (1 - α) * CVSS_norm
其中 CVSS_norm = CVSS_base / 10.0
```

**参数**:
- `p_vuln`: 模型预测的漏洞概率 [0, 1]
- `cvss_base_score`: CVSS基础评分 [0, 10]（可选）
- `alpha`: 权重参数，默认0.6（ML预测权重60%，CVSS权重40%）

**逻辑**:
- 如果提供CVSS评分，使用加权公式
- 如果未提供CVSS，直接使用`p_vuln`作为风险评分

#### 5.2.2 CVSS估算机制

**相似度估算** (`estimate_cvss_from_similarity`):
1. 使用TF-IDF向量计算输入文本与训练数据的余弦相似度
2. 找到top-k最相似的训练样本（默认k=5）
3. 对相似样本的CVSS值进行加权平均（权重=相似度）
4. 如果最大相似度 < 阈值（默认0.18），返回None（避免误匹配）

**概率映射估算** (`estimate_cvss_from_p_vuln`):
- 当相似度估算失败时的后备方法
- 使用分段线性映射将`p_vuln`转换为CVSS评分
- 仅当`p_vuln`较高时使用（避免低概率误报）

#### 5.2.3 适用性评估（Business Clipping）

**目的**: 对明显非漏洞语境文本，强制返回`applicable=false`，避免无关文本被错误评分

**裁剪条件**:
1. **EMPTY_TEXT**: 文本长度 < 最小长度（默认20字符）
2. **LOW_SIMILARITY**: 与训练数据相似度 < 阈值（默认0.18）且用户未提供CVSS
3. **LOW_SIGNAL**: TF-IDF非零特征数 < 阈值（默认3）且用户未提供CVSS
4. **LOW_PVULN**: 漏洞概率 < 阈值（默认0.10）且相似度低

**配置参数**:
- `CLIP_NA_ENABLED`: 是否启用裁剪（默认true）
- `CLIP_PVULN_THRESHOLD`: p_vuln阈值（默认0.10）
- `CLIP_SIM_THRESHOLD`: 相似度阈值（默认0.18）
- `CLIP_MIN_TEXT_LEN`: 最小文本长度（默认20）
- `CLIP_MIN_NONZERO_TFIDF`: 最小非零特征数（默认3）

### 5.3 风险等级分类

#### 5.3.1 等级定义

**Low (低风险)**:
- `riskScore < 0.4`
- 对应CVSS评分 [0, 4.0)
- 影响范围小，可利用性低

**Medium (中风险)**:
- `0.4 <= riskScore < 0.6`
- 对应CVSS评分 [4.0, 7.0)
- 有一定影响，需要关注

**High (高风险)**:
- `0.6 <= riskScore < 0.8`
- 对应CVSS评分 [7.0, 9.0)
- 影响较大，需要优先处理

**Critical (严重风险)**:
- `riskScore >= 0.8`
- 对应CVSS评分 [9.0, 10.0]
- 影响严重，需要立即处理

#### 5.3.2 兼容性字段

为了向后兼容旧系统，提供以下映射：

**旧字段 → 新字段**:
- `pVuln = P(High) + P(Critical)`（高风险概率）
- `riskLevel = severityLevel`（风险等级）
- `cvss = cvss_base_score`（CVSS评分）

**新字段优先**: 系统优先使用新字段（`pApplicable`, `applicable`, `severityLevel`, `severityProbs`），如果不存在则fallback到旧字段。

---

## 6. 系统集成与部署

### 6.1 API接口设计

#### 6.1.1 单样本预测接口

**端点**: `POST /api/predictions`

**请求体**:
```json
{
  "sample": {
    "sample_id": "sample_1",
    "text_description": "Cross-Site Scripting (XSS) vulnerability...",
    "cvss_base_score": 7.5  // 可选
  },
  "app_model_path": "models/app_model_001_xgb",  // 可选
  "sev_model_path": "models/sev_model_001",      // 可选
  "app_threshold": 0.5                           // 可选
}
```

**响应**:
```json
{
  "sample_id": "sample_1",
  "applicable": true,
  "pApplicable": 0.95,
  "severityLevel": "High",
  "severityProbs": {
    "Low": 0.05,
    "Medium": 0.15,
    "High": 0.65,
    "Critical": 0.15
  },
  "riskScore": 0.72,
  "pVuln": 0.80,
  "riskLevel": "High",
  "explanation": "Vulnerability-related text detected. Predicted severity: High...",
  "text_len": 156
}
```

#### 6.1.2 批量预测接口

**端点**: `POST /api/predictions/batch`

**请求体**:
```json
{
  "samples": [
    {
      "sample_id": "sample_1",
      "text_description": "..."
    },
    {
      "sample_id": "sample_2",
      "text_description": "..."
    }
  ]
}
```

**响应**:
```json
{
  "predictions": [
    {
      "sample_id": "sample_1",
      "applicable": true,
      "severityLevel": "High",
      "riskScore": 0.72,
      ...
    },
    ...
  ],
  "summary": {
    "total": 100,
    "low": 20,
    "medium": 40,
    "high": 30,
    "critical": 10
  }
}
```

### 6.2 Docker部署

#### 6.2.1 Docker Compose配置

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: vulnrisk
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend-node
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/vulnrisk
    depends_on:
      - postgres

  ml-service:
    build: ./ml-service
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data

  frontend:
    build: ./frontend-vue
    ports:
      - "80:80"
    depends_on:
      - backend
```

#### 6.2.2 启动命令

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 6.3 模型管理

#### 6.3.1 模型训练命令

**训练Applicability模型**:
```bash
# XGBoost模型（推荐）
python ml-service/train_applicability_model.py --model xgb

# RandomForest模型
python ml-service/train_applicability_model.py --model rf

# LogisticRegression模型（基线）
python ml-service/train_applicability_model.py --model lr

# 自定义采样数
python ml-service/train_applicability_model.py --model xgb --max-pos 50000 --max-neg 10000
```

**训练Severity模型**:
```bash
python ml-service/train_severity_model.py ml-service/train_severity_config.json
```

#### 6.3.2 模型注册命令

```bash
# 注册模型到数据库
node backend-node/scripts/register-model.js models/app_model_001_xgb app_model_001_xgb
```

#### 6.3.3 模型激活

通过前端界面或API激活模型：
- 访问 `/api/models/{modelId}/activate`
- 系统自动将旧模型设为非激活状态

---

## 7. 性能评估与实验结果

### 7.1 Applicability模型性能

#### 7.1.1 XGBoost模型

**测试集性能** (19,847样本):
- **ROC-AUC**: 0.9992 (99.92%)
- **PR-AUC**: 0.9993 (99.93%)
- **Accuracy**: 0.9889 (98.89%)
- **Precision**: 0.9971 (99.71%)
- **Recall**: 0.9808 (98.08%)
- **F1-Score**: 0.9889 (98.89%)

**混淆矩阵分析**:
- True Negative (TN): 正确识别负类（非漏洞文本）
- False Positive (FP): 误报（将非漏洞文本识别为漏洞）
- False Negative (FN): 漏报（将漏洞文本识别为非漏洞）
- True Positive (TP): 正确识别正类（漏洞文本）

**性能分析**:
- 模型达到了极高的性能，ROC-AUC接近1.0
- 说明正类和负类数据区分度很好
- 误报率和漏报率都很低（< 2%）
- 适合生产环境使用

#### 7.1.2 RandomForest模型

**测试集性能** (19,847样本):
- **ROC-AUC**: 0.9993 (99.93%)
- **PR-AUC**: 0.9994 (99.94%)
- **Accuracy**: 0.9921 (99.21%)
- **Precision**: 0.9954 (99.54%)
- **Recall**: 0.9889 (98.89%)
- **F1-Score**: 0.9921 (99.21%)

**对比分析**:
- RandomForest略优于XGBoost（ROC-AUC高0.0001）
- 两个模型性能差异很小（< 0.1%）
- 都可以作为生产模型使用

### 7.2 Severity模型性能

#### 7.2.1 整体性能

**测试集性能** (18,940样本):
- **Accuracy**: 0.7185 (71.85%)
- **Macro Precision**: 0.7603 (76.03%)
- **Macro Recall**: 0.5328 (53.28%)
- **Macro F1**: 0.5856 (58.56%)
- **Weighted F1**: 0.7040 (70.40%)

**分析**:
- 整体准确率71.85%，说明模型有一定预测能力
- Macro F1为58.56%，受类别不平衡影响
- Weighted F1为70.40%，说明多数类（Medium）表现较好

#### 7.2.2 每类性能分析

**Medium类（多数类）**:
- Precision: 0.7360, Recall: 0.8552, F1: 0.7912
- 表现最好，因为样本数量多（51.8%）

**High类**:
- Precision: 0.6761, Recall: 0.6731, F1: 0.6746
- 表现中等，平衡较好

**Low类和Critical类（少数类）**:
- Low: Precision 0.8567, Recall 0.2747, F1 0.4160
- Critical: Precision 0.7724, Recall 0.3282, F1 0.4607
- 召回率低，存在欠拟合问题

**改进建议**:
1. 使用SMOTE对少数类进行过采样
2. 调整类别权重（class_weight）
3. 使用focal loss等损失函数
4. 增加少数类样本数量

### 7.3 端到端系统性能

#### 7.3.1 预测延迟

**单样本预测**:
- Stage A (Applicability): ~50-100ms
- Stage B (Severity): ~50-100ms
- 总延迟: ~100-200ms

**批量预测** (100样本):
- 总延迟: ~2-5秒
- 平均每样本: ~20-50ms

#### 7.3.2 系统吞吐量

- **单样本预测**: ~10-20 requests/second
- **批量预测**: ~5-10 requests/second（取决于批量大小）

**优化建议**:
1. 使用模型缓存减少加载时间
2. 批量向量化提高效率
3. 使用GPU加速（如果可用）

---

## 8. 技术栈与依赖

### 8.1 前端依赖

```json
{
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.2.4",
    "pinia": "^2.1.6",
    "element-plus": "^2.3.8",
    "echarts": "^5.4.2",
    "axios": "^1.5.0"
  }
}
```

### 8.2 后端依赖

```json
{
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.6.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
}
```

### 8.3 ML服务依赖

```txt
flask==2.3.3
flask-cors==4.0.0
scikit-learn==1.3.2
xgboost==2.0.2
pandas==2.1.3
numpy==1.24.3
joblib==1.3.2
imbalanced-learn==0.11.0
```

---

## 9. 未来改进方向

### 9.1 模型性能优化

1. **Severity模型改进**:
   - 使用SMOTE处理类别不平衡
   - 尝试深度学习模型（BERT, RoBERTa）
   - 特征工程优化（添加CVSS特征）

2. **Applicability模型优化**:
   - 增加负类样本多样性
   - 尝试更复杂的特征（n-gram扩展到3-gram）
   - 集成学习（Ensemble）

### 9.2 系统功能扩展

1. **实时监控**:
   - 模型性能监控
   - 预测结果统计分析
   - 异常检测

2. **可解释性**:
   - SHAP值分析
   - 特征重要性可视化
   - 预测原因解释

3. **自动化**:
   - 自动模型重训练
   - 自动阈值调优
   - A/B测试框架

### 9.3 数据质量提升

1. **数据增强**:
   - 数据清洗优化
   - 噪声过滤
   - 数据验证机制

2. **数据标注**:
   - 半监督学习
   - 主动学习
   - 众包标注

### 9.4 工程优化

1. **性能优化**:
   - 模型量化
   - 模型剪枝
   - 缓存机制

2. **可扩展性**:
   - 分布式训练
   - 模型服务化（Model Serving）
   - 微服务架构优化

---

## 附录

### A. 模型文件结构

```
models/
├── app_model_001_lr/          # LogisticRegression模型
│   ├── model.joblib
│   ├── vectorizer.joblib
│   ├── metadata.json
│   └── eval_predictions.csv
├── app_model_001_xgb/         # XGBoost模型（推荐）
│   ├── model.joblib
│   ├── vectorizer.joblib
│   ├── metadata.json
│   └── eval_predictions.csv
├── app_model_001_rf/          # RandomForest模型
│   ├── model.joblib
│   ├── vectorizer.joblib
│   ├── metadata.json
│   └── eval_predictions.csv
└── sev_model_001/             # Severity模型
    ├── model.joblib
    ├── vectorizer.joblib
    ├── metadata.json
    └── training_predictions.csv
```

### B. 关键配置文件

**训练配置示例** (`train_severity_config.json`):
```json
{
  "csv_path": "ml-service/merged_json_table.csv",
  "model_type": "XGBoost",
  "use_smote": false,
  "test_size": 0.2,
  "random_state": 42,
  "output_dir": "models/sev_model_001",
  "text_column": "description_clean",
  "max_features": 20000
}
```

### C. 环境变量配置

```env
# 数据库
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vulnrisk

# GitHub API
GITHUB_TOKEN=your_token_here
GITHUB_DISABLE_LOCAL_FILTER=false

# JWT
JWT_SECRET=your_secret_here

# ML服务
MODELS_DIR=/app/models
DATA_DIR=/app/data
RISK_ALPHA=0.6
CVSS_SIM_THRESHOLD=0.18
CLIP_NA_ENABLED=true
```

---

**报告生成时间**: 2026年2月4日  
**报告版本**: 2.0.0  
**维护者**: VulnRisk Development Team

---

## 总结

本报告详细介绍了VulnRisk漏洞风险评估平台的技术实现，重点阐述了：

1. **模型训练部分**:
   - Stage A Applicability模型（LR/XGB/RF三种算法）的详细配置和性能
   - Stage B Severity模型（4类多分类）的训练流程和评估结果
   - 模型注册与激活机制

2. **风险评估部分**:
   - 两阶段推理流程的完整实现
   - 风险评分计算公式和等级映射
   - CVSS估算机制和适用性评估（Business Clipping）

3. **系统集成**:
   - API接口设计
   - Docker部署方案
   - 模型管理流程

系统已经实现了完整的漏洞风险评估功能，Applicability模型达到了极高的性能（ROC-AUC > 0.99），Severity模型在多数类上表现良好（Medium类F1=0.79），整体系统可以投入生产使用。未来可以通过类别不平衡处理、深度学习模型等方法进一步优化Severity模型的性能。


