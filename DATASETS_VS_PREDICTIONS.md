# Datasets vs Predictions 数据格式对比

## 概述

系统中有两个不同的数据上传功能，它们有不同的用途和数据格式要求：

1. **Datasets（数据集）** - 用于训练机器学习模型
2. **Predictions（批量预测）** - 用于使用已训练模型进行预测

## 主要区别

| 特性 | Datasets | Predictions |
|------|----------|-------------|
| **用途** | 训练模型 | 使用模型进行预测 |
| **必需字段** | `sample_id`, `text_description` | `sample_id` (或 `id`), `text_description` (或 `description`) |
| **标签字段** | ✅ **必需** `label` (0或1) | ❌ 不需要 |
| **CVSS字段** | 可选 `cvss_base_score` | 可选 `cvss_base_score` (或 `cvss`) |
| **支持格式** | JSON, CSV | JSON, CSV, Excel (.xlsx, .xls) |
| **数据存储** | 保存到数据库和文件系统 | 仅用于预测，不保存为数据集 |
| **后续处理** | 可用于模型训练 | 直接进行预测并保存结果 |

## Datasets（数据集）数据格式

### 用途
- 用于训练机器学习模型
- 数据需要包含**标签（label）**，用于监督学习
- 上传后可以用于模型训练

### 必需字段

1. **`sample_id`** (string) - 样本ID
2. **`text_description`** (string) - 漏洞描述文本
3. **`label`** (number) - **标签，必需！** 
   - `0` = 非漏洞
   - `1` = 漏洞

### 可选字段

- **`cvss_base_score`** (number) - CVSS基础评分 (0-10)

### 数据格式示例

#### JSON格式
```json
[
  {
    "sample_id": "sample_001",
    "text_description": "SQL injection vulnerability in user authentication",
    "cvss_base_score": 9.8,
    "label": 1
  },
  {
    "sample_id": "sample_002",
    "text_description": "Secure authentication mechanism implemented",
    "cvss_base_score": 0.0,
    "label": 0
  }
]
```

#### CSV格式
```csv
sample_id,text_description,cvss_base_score,label
sample_001,"SQL injection vulnerability in user authentication",9.8,1
sample_002,"Secure authentication mechanism implemented",0.0,0
```

### 关键特点

- ✅ **必须包含 `label` 字段** - 这是训练数据的关键
- ✅ 数据会被保存到数据库和文件系统
- ✅ 可以用于后续的模型训练
- ✅ 支持预处理功能

## Predictions（批量预测）数据格式

### 用途
- 使用已训练的模型进行预测
- 数据是**未知标签**的，需要模型来预测
- 上传后直接进行预测，结果保存到数据库

### 必需字段

1. **`sample_id`** (或 `id`, `sampleId`, `Sample ID`) - 样本ID
2. **`text_description`** (或 `description`, `textDescription`, `Text Description`) - 漏洞描述文本

### 可选字段

- **`cvss_base_score`** (或 `cvss`, `cvssBaseScore`, `CVSS Base Score`) - CVSS基础评分 (0-10)

### 数据格式示例

#### JSON格式（对象数组）
```json
[
  {
    "sample_id": "CVE-2024-0001",
    "text_description": "A buffer overflow vulnerability exists in the authentication module",
    "cvss_base_score": 7.5
  },
  {
    "sample_id": "CVE-2024-0002",
    "text_description": "Cross-site scripting vulnerability in user input validation"
  }
]
```

#### JSON格式（samples对象）
```json
{
  "samples": [
    {
      "sample_id": "CVE-2024-0001",
      "text_description": "A buffer overflow vulnerability exists...",
      "cvss_base_score": 7.5
    }
  ]
}
```

#### CSV格式
```csv
sample_id,text_description,cvss_base_score
CVE-2024-0001,"A buffer overflow vulnerability exists...",7.5
CVE-2024-0002,"Cross-site scripting vulnerability...",
```

#### Excel格式
| sample_id | text_description | cvss_base_score |
|-----------|-----------------|----------------|
| CVE-2024-0001 | A buffer overflow vulnerability... | 7.5 |
| CVE-2024-0002 | Cross-site scripting vulnerability... | |

### 关键特点

- ❌ **不需要 `label` 字段** - 这是要预测的数据
- ✅ 支持多种文件格式（JSON, CSV, Excel）
- ✅ 字段名称更灵活（支持多种变体）
- ✅ 预测结果会保存到数据库
- ✅ 可以查看预测详情和报告

## 字段对比表

| 字段名 | Datasets | Predictions | 说明 |
|--------|----------|-------------|------|
| `sample_id` | ✅ 必需 | ✅ 必需 | 样本标识符 |
| `text_description` | ✅ 必需 | ✅ 必需 | 漏洞描述文本 |
| `label` | ✅ **必需** | ❌ 不需要 | 训练标签（0/1） |
| `cvss_base_score` | 可选 | 可选 | CVSS评分 |

## 使用场景

### 何时使用 Datasets？

- ✅ 您有**已标注的数据**（知道哪些是漏洞，哪些不是）
- ✅ 想要**训练新的模型**
- ✅ 数据包含 `label` 字段（0或1）
- ✅ 数据将用于机器学习训练

### 何时使用 Predictions？

- ✅ 您有**未标注的数据**（不知道是否是漏洞）
- ✅ 想要使用**已训练的模型**进行预测
- ✅ 数据**不包含** `label` 字段
- ✅ 需要快速获得预测结果

## 数据流转

### Datasets 数据流
```
上传文件 → 解析数据 → 保存到数据库 → 保存到文件系统 → 可用于训练模型
```

### Predictions 数据流
```
上传文件 → 解析数据 → 调用ML服务预测 → 保存预测结果到数据库 → 可查看预测报告
```

## 常见问题

### Q1: 我可以把训练数据用于预测吗？

**可以**，但需要注意：
- 训练数据包含 `label` 字段，预测时不需要
- 可以手动移除 `label` 字段，或者系统会自动忽略

### Q2: 我可以把预测数据用于训练吗？

**可以**，但需要：
- 添加 `label` 字段（0或1）
- 确保数据质量符合训练要求
- 通过 Datasets 上传

### Q3: 为什么 Datasets 不支持 Excel？

Datasets 主要用于训练，通常数据量较大，CSV 和 JSON 格式更通用。如果需要，可以先将 Excel 转换为 CSV。

### Q4: 字段名称必须完全匹配吗？

- **Datasets**: 字段名称需要精确匹配（`sample_id`, `text_description`, `label`, `cvss_base_score`）
- **Predictions**: 支持多种字段名称变体，更灵活

## 总结

| 特性 | Datasets | Predictions |
|------|----------|-------------|
| **核心区别** | 包含标签，用于训练 | 无标签，用于预测 |
| **必需字段** | `sample_id`, `text_description`, **`label`** | `sample_id`, `text_description` |
| **数据用途** | 训练模型 | 模型预测 |
| **结果** | 可用于训练 | 预测结果保存到数据库 |

**关键记忆点**：
- 📚 **Datasets = 训练数据 = 需要标签**
- 🔮 **Predictions = 预测数据 = 不需要标签**









