# 修复总结：强制两阶段流程，彻底解决正样本被判 Not Applicable 问题

## 修复目标
确保所有预测入口强制走两阶段流程（Stage A + Stage B），彻底禁止 legacy 的 LOW_PVULN gate 影响 applicable 判定。

## 修改的文件

### 1. `ml-service/app.py`

#### A. `/predict` 端点（单样本预测）
- **修改前**：根据条件判断是否使用两阶段模型，可能回退到 legacy 流程
- **修改后**：
  - 默认强制使用两阶段推理
  - 仅当明确指定 `legacy_mode=true` 时才走旧流程
  - 自动检测并使用默认模型路径（优先使用 `app_model_002_aug_xgb`）

#### B. `/predict/batch` 端点（批量预测）
- **修改前**：完全走 legacy 流程，使用旧模型
- **修改后**：
  - 强制使用两阶段推理，不再使用 legacy 流程
  - 自动检测并使用默认模型路径
  - 对每个样本调用 `predict_two_stage()`

#### C. Legacy 批量预测中的 gating 逻辑（`/predict/batch/legacy`）
- **修改前**：使用 `LOW_PVULN`、`LOW_SIMILARITY`、`OOD` 等中文解释
- **修改后**：
  - 所有 gating reason 统一改为 `NOT_VULNERABILITY_TEXT`
  - 所有 `risk_level` 从 `'N/A'` 改为 `'Unknown'`
  - 所有 explanation 改为英文标准文案：
    `"This input text was not identified as vulnerability-related and therefore did not enter the risk assessment stage."`
  - 添加注释说明：此逻辑仅用于 legacy 批量预测，不影响两阶段预测的 applicable 判定

### 2. `backend-node/src/services/prediction.service.ts`

#### A. `predict()` 方法（单样本预测）
- **修改前**：传递 `model_path` 参数，可能触发 legacy 流程
- **修改后**：
  - 传递 `app_model_path` 和 `sev_model_path` 参数
  - 强制使用两阶段预测
  - `app_threshold` 默认值为 0.5

#### B. `batchPredict()` 方法（批量预测）
- **修改前**：传递 `model_path` 参数，调用 legacy 批量预测
- **修改后**：
  - 传递 `app_model_path` 和 `sev_model_path` 参数
  - 强制使用两阶段批量预测
  - `app_threshold` 默认值为 0.5

## 关键修复点

### 1. 强制两阶段流程
- `/predict` 端点默认强制走两阶段，除非明确指定 `legacy_mode=true`
- `/predict/batch` 端点完全改为两阶段批量预测
- backend-node 调用时传递正确的两阶段模型路径

### 2. 移除 LOW_PVULN gate 对 applicable 的影响
- 两阶段预测中，`applicable` 仅由 `pApplicable` 与 `app_threshold` 决定
- Legacy 批量预测中的 gating 逻辑已更新，但仅用于向后兼容，不影响两阶段预测

### 3. 统一字段和解释文案
- `applicable=false` 时：
  - `reason` 统一为 `"NOT_VULNERABILITY_TEXT"`
  - `riskLevel` 统一为 `"Unknown"`（不再使用 `"N/A"`）
  - `riskScore` 统一为 `0`
  - `severityLevel` 和 `severityProbs` 为 `null`
  - `explanation` 使用标准英文文案
- `applicable=true` 时：
  - 必须返回 `severityProbs`、`severityLevel`、`riskScore`、`riskLevel`
  - `explanation` 使用标准英文文案

## 端点/函数现在固定走两阶段

1. **`/predict`**（单样本预测）
   - 默认强制走两阶段
   - 仅当 `legacy_mode=true` 时走旧流程

2. **`/predict/batch`**（批量预测）
   - 完全改为两阶段批量预测
   - 不再使用 legacy 流程

3. **`backend-node/src/services/prediction.service.ts::predict()`**
   - 传递两阶段模型路径，强制走两阶段

4. **`backend-node/src/services/prediction.service.ts::batchPredict()`**
   - 传递两阶段模型路径，强制走两阶段批量预测

## 示例响应片段

### 正样本（CVE-like 文本）
```json
{
  "sample_id": "POS-001",
  "applicable": true,
  "pApplicable": 0.95,
  "severityLevel": "High",
  "severityProbs": {
    "Low": 0.1,
    "Medium": 0.2,
    "High": 0.5,
    "Critical": 0.2
  },
  "riskScore": 0.75,
  "riskLevel": "High",
  "explanation": "This input text was identified as vulnerability-related. A conditional risk assessment was performed based on learned vulnerability patterns. Predicted severity tendency: High. The risk score reflects a reference estimate under uncertainty.",
  "reliability": "Medium",
  "reason": null
}
```

### 负样本（普通 GitHub issue）
```json
{
  "sample_id": "NEG-001",
  "applicable": false,
  "pApplicable": 0.15,
  "severityLevel": null,
  "severityProbs": null,
  "riskScore": 0,
  "riskLevel": "Unknown",
  "explanation": "This input text was not identified as vulnerability-related and therefore did not enter the risk assessment stage.",
  "reliability": "Low",
  "reason": "NOT_VULNERABILITY_TEXT"
}
```

## 验收标准

1. ✅ 所有入口（单条预测 + 批量预测）强制走两阶段流程
2. ✅ 彻底禁止 legacy 的 LOW_PVULN gate 影响 applicable 判定
3. ✅ 对 CVE-like 正样本文本：applicable 必须为 true，并进入 Stage B 输出 severityProbs / severityLevel / riskScore
4. ✅ 对 negatives 与 augmented_negatives：仍应 applicable=false（保持现有效果）
5. ✅ explanation 必须为英文，且不再引用 pVuln < threshold 的拒绝逻辑

## 注意事项

1. Legacy 批量预测端点（`/predict/batch/legacy`）仍然保留，但仅用于向后兼容
2. 两阶段预测的 `applicable` 判定完全由 `pApplicable` 与 `app_threshold` 决定，不受其他因素影响
3. 所有解释文案已统一为英文，符合论文答辩要求


