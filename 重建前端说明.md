# 重新构建前端容器以应用排序功能

## 问题原因
前端代码被构建成静态文件并打包到Docker镜像中，仅重启容器不会重新构建代码。

## 解决方案

### 方法1：重新构建前端容器（推荐）

```bash
# 停止并删除前端容器
docker-compose stop frontend-vue
docker-compose rm -f frontend-vue

# 重新构建前端镜像（不使用缓存）
docker-compose build --no-cache frontend-vue

# 启动前端容器
docker-compose up -d frontend-vue
```

### 方法2：一键重建所有服务

```bash
# 停止所有服务
docker-compose down

# 重新构建所有镜像（不使用缓存）
docker-compose build --no-cache

# 启动所有服务
docker-compose up -d
```

### 方法3：仅重建前端（最快）

```bash
# 直接重建并启动前端服务
docker-compose up -d --build frontend-vue
```

## 验证

重建完成后：
1. 清除浏览器缓存（Ctrl+F5 或 Cmd+Shift+R）
2. 访问 Predictions 页面
3. 点击 "Risk Level" 列头，应该能看到排序图标
4. 点击后数据应该按 Low → Medium → High → Critical 排序

## 已修复的代码

已添加：
- `prop="riskLevel"` - 指定排序字段
- `sortable` - 启用排序
- `:sort-method="sortRiskLevel"` - 自定义排序方法

排序顺序：
- Low (1) < Medium (2) < High (3) < Critical (4) < Unknown/N/A (5)




