# 通用门控机制更新总结

## 概述

将保守门控机制更新为**通用、内容无关**的实现，使用证据充分性检查而非硬编码特定测试字符串。

## 修改文件

### `ml-service/app.py`

## 核心改进

### 1. 通用证据提取函数 (`extract_evidence()`)

**位置**: 第186-271行

**功能**:
- **安全关键词统计**: 检测40+种常见漏洞类型关键词（XSS, SQLi, RCE, CSRF等）
- **上下文线索统计**: 检测exploit/attack/impact相关词汇（attacker, remote, execute, payload, bypass等）
- **技术证据模式匹配**: 使用正则表达式检测：
  - CVE IDs (`CVE-2024-1234`)
  - 版本号 (`2.5.0`)
  - 产品/组件名称（apache, nginx, mysql等）
  - 堆栈跟踪指示符
  - 错误代码
  - 代码样标记（函数调用、赋值）
  - 文件路径
  - URLs
  - PoC指示符
  - 受影响组件词汇

**返回**: `(security_keyword_count, context_cue_count, technical_evidence_count)`

### 2. 改进的输入质量检查 (`check_input_quality()`)

**位置**: 第274-325行

**新增检查5: 证据充分性检查**
- 如果 `security_keyword_count > 0` 但 `total_evidence < 1`（上下文线索+技术证据）
- 触发条件：检测到安全关键词但缺乏exploit/impact/context证据
- 输出：`applicable=false`, `reliability=Low`, 明确的说明note

**保留的检查**:
- 检查1: 文本过短（<15字符）
- 检查2: 高度重复文本
- 检查3: 问候语/闲聊模式
- 检查4: 字符重复

### 3. Stage A后证据充分性检查

**位置**: 第507-532行（在Stage A通过后、Stage B之前）

**功能**:
- 即使Stage A模型判断`applicable=true`，如果证据不足，保守跳过Stage B
- 双重保护：precheck + Stage A后检查
- 确保不会对仅有安全关键词但无上下文的输入进行评分

### 4. 改进的不确定性评估 (`assess_prediction_uncertainty()`)

**位置**: 第356-404行

**改进**:
- **检查1**: top-1和top-2概率差距（margin < 0.15）
- **检查2**: 概率分布熵（normalized_entropy > 0.85）
- **检查3**: 最大概率是否足够高（max_prob < 0.4）

**返回**: `(is_uncertain, uncertainty_level, uncertainty_reason)`
- 新增`uncertainty_reason`字段，提供明确的降级原因说明

**行为**: 只降级可靠性，**不改变**预测的严重度等级、概率或风险评分

### 5. 补丁/缓解文本检测（保持不变）

**位置**: 第328-353行

- 检测修复/缓解关键词
- 降级可靠性
- 添加说明note
- 标记`inputType='patch_mitigation'`

## 测试验证结果

### 测试用例1: "hello hello, nice to meet you"
- ✅ **结果**: `is_low_quality=True`, `reason='GREETING_LIKE'`
- ✅ **行为**: 被输入质量检查拒绝（检查3）

### 测试用例2: "TODAY IS A GOOD DAY XSS"
- ✅ **结果**: `is_low_quality=True`, `reason='INSUFFICIENT_EVIDENCE'`
- ✅ **证据统计**: 安全关键词=1, 上下文线索=0, 技术证据=0
- ✅ **行为**: 检测到安全关键词但证据不足，保守跳过

### 测试用例3: "Fix: Prevent command injection..."
- ✅ **结果**: `is_low_quality=False`
- ✅ **证据统计**: 安全关键词=1, 上下文线索=2, 技术证据=0
- ✅ **行为**: 有足够的上下文线索，通过检查（但会被补丁检测标记）

### 测试用例4: "CVE-2024-1234: SQL injection in version 2.5.0..."
- ✅ **结果**: `is_low_quality=False`
- ✅ **证据统计**: 安全关键词=3, 上下文线索=5, 技术证据=2
- ✅ **行为**: 丰富的证据，完全通过检查

### 测试用例5: 正常CVE漏洞描述
- ✅ **结果**: `is_low_quality=False`
- ✅ **行为**: 完全正常输出，不受影响

### 不确定性评估测试

- ✅ 均匀分布 `[0.25, 0.25, 0.25, 0.25]`: 检测为不确定（margin=0.000）
- ✅ top-1和top-2接近 `[0.3, 0.28, ...]`: 检测为不确定（margin=0.020 < 0.15）
- ✅ 有明显优势 `[0.5, 0.3, ...]`: 不确定=False
- ✅ 接近但margin足够 `[0.35, 0.33, ...]`: 检测为不确定（margin=0.020 < 0.15）

## 关键特性

### 1. 通用性
- ✅ **无硬编码测试字符串**: 不特殊处理"TODAY IS A GOOD DAY XSS"等
- ✅ **基于证据统计**: 使用通用的关键词和模式匹配
- ✅ **内容无关**: 适用于任何用户输入

### 2. 保守性
- ✅ **双重检查**: precheck + Stage A后检查
- ✅ **阈值保守**: 至少需要1个上下文线索或技术证据
- ✅ **不改变模型输出**: 只添加元数据和可靠性标记

### 3. 可解释性
- ✅ **明确的notes**: 所有拒绝和降级都有详细说明
- ✅ **证据统计**: 在note中显示具体的证据计数
- ✅ **不确定性原因**: 明确说明为什么降级可靠性

### 4. 向后兼容
- ✅ **所有现有字段保持不变**
- ✅ **新字段都是可选的**
- ✅ **不影响正常CVE类输入**

## API响应字段

### 新增/更新的字段（可选）:
- `reliability`: 'High' | 'Medium' | 'Low'
- `notes`: string[] | null
- `inputType`: 'normal' | 'patch_mitigation' | 'low_quality'
- `modelInfo`: { applicabilityModel, severityModel }

### 保持不变:
- `applicable`, `pApplicable`, `severityLevel`, `severityProbs`
- `riskScore`, `riskLevel`, `pVuln`
- `explanation`, `reason`

## 论文答辩适用性

这些改进确保了：

1. **运行时行为一致性**: 
   - 不会对明显非漏洞文本（如"TODAY IS A GOOD DAY XSS"）给出高风险评分
   - 不会对仅有安全关键词但无上下文的输入进行评分

2. **通用性**:
   - 不依赖特定测试用例
   - 适用于任何用户输入

3. **可解释性**:
   - 所有决策都有明确的证据统计和说明
   - 不确定性评估有具体的原因

4. **研究完整性**:
   - 不改变模型训练或评估结果
   - 只添加系统级处理机制

---

**实施日期**: 2026-02-04  
**修改者**: AI Assistant  
**验证状态**: ✅ 测试通过


