# 字段重命名重构总结

## 重构日期
2026-02-10

## 重构目标
将 Stage A 的 applicability 相关字段重命名为更贴近论文标题的"漏洞相关性预测"命名，同时保持完全向后兼容。

## 字段映射

### 核心字段
- `pApplicable` → `pVulnRelated` (主字段)
- `applicable` → `isVulnRelated` (主字段)

### 配置字段
- `app_threshold` → `relevance_threshold` (主字段)
- `app_model_path` → `relevance_model_path` (主字段)
- `model_function: "applicability"` → `model_function: "vuln_relevance"` (主字段)

## 兼容性策略

### 输出兼容
所有对外 JSON 响应同时包含新旧字段，值完全相同：
- `pVulnRelated` 与 `pApplicable` 值完全相同
- `isVulnRelated` 与 `applicable` 值完全相同
- 新字段作为主字段用于前端展示/文档示例
- 旧字段保留用于兼容旧客户端与历史脚本

### 解析兼容
任何读取 Stage A 字段的地方，优先读新字段，不存在则回退旧字段：
- 优先 `pVulnRelated` / `isVulnRelated`
- fallback `pApplicable` / `applicable`
- 不改变任何默认值、阈值判断或错误处理分支

## 修改的文件清单

### 后端 Python

#### 1. `ml-service/app.py`
**修改位置：**
- `predict_two_stage()` 函数（第 419-656 行）
  - 所有返回字典中添加新字段 `isVulnRelated`, `pVulnRelated`
  - 同时保留旧字段 `applicable`, `pApplicable`（兼容）
  - 函数参数添加 `relevance_model_path`, `relevance_threshold`（新参数名）
  - 保持参数 `app_model_path`, `app_threshold`（兼容）
  - 逻辑保持不变：`applicable = p_applicable >= app_threshold`

- API 端点 `/predict` 和 `/predict/batch`
  - 支持新字段名 `relevance_model_path`, `relevance_threshold`
  - 同时支持旧字段名 `app_model_path`, `app_threshold`（兼容）
  - 优先使用新字段名，fallback 到旧字段名

- `modelInfo` 字段
  - 添加 `relevanceModel`（新字段）
  - 保留 `applicabilityModel`（旧字段，兼容）

#### 2. `ml-service/risk.py`
**修改位置：**
- `assess_applicability()` 函数（第 223-345 行）
  - 所有返回字典中添加新字段 `isVulnRelated`
  - 同时保留旧字段 `applicable`（兼容）
  - 更新函数文档字符串

#### 3. `ml-service/train_applicability_model.py`
**修改位置：**
- metadata 生成（第 669-670 行）
  - `model_function`: `'applicability'` → `'vuln_relevance'`（主字段）
  - `task`: `'applicability_classification'` → `'vuln_relevance_classification'`（主字段）
  - 添加注释说明旧字段已重命名

- 测试集预测结果（第 737 行）
  - 添加 `p_vuln_related` 字段（新字段）
  - 保留 `p_applicable` 字段（兼容）

### 后端 Node.js

#### 4. `backend-node/src/services/prediction.service.ts`
**修改位置：**
- `predict()` 方法（第 20-145 行）
  - 从 ML 服务响应优先提取新字段 `pVulnRelated`, `isVulnRelated`
  - fallback 到旧字段 `pApplicable`, `applicable`
  - 存储到 metadata 时同时存储新旧字段（值相同）
  - 返回响应时同时包含新旧字段

- `batchPredict()` 方法（第 147-245 行）
  - 批量预测时优先读取新字段，兼容旧字段
  - metadata 存储同时包含新旧字段

- `getPredictions()` 方法（第 247-287 行）
  - 从 metadata 提取时优先新字段，fallback 旧字段
  - 返回时同时包含新旧字段

- `getPredictionById()` 方法（第 289-316 行）
  - 从 metadata 提取时优先新字段，fallback 旧字段
  - 返回时同时包含新旧字段

- `getAllPredictionsForExport()` 方法（第 327-353 行）
  - 提取时优先新字段，fallback 旧字段
  - 返回对象包含新旧字段

- `exportToCSV()` 方法（第 355-385 行）
  - CSV 表头改为 `isVulnRelated`, `pVulnRelated`
  - 数据导出使用新字段名

- `exportToExcel()` 方法（第 387-407 行）
  - Excel 列名改为 `isVulnRelated`, `pVulnRelated`
  - 数据导出使用新字段名

- ML 服务请求
  - 同时发送新旧参数名（`relevance_model_path` + `app_model_path`）
  - 同时发送新旧阈值参数（`relevance_threshold` + `app_threshold`）

#### 5. `backend-node/scripts/query-db.js`
**修改位置：**
- 预测结果详情打印（第 108-109 行）
  - 使用新字段名 `isVulnRelated`, `pVulnRelated`
  - 兼容旧字段读取

- 两阶段预测统计（第 125-136 行）
  - 统计字段名改为 `isVulnRelated`, `notVulnRelated`
  - 兼容旧字段读取

### 数据库

#### 6. `query.sql`
**修改位置：**
- 查询示例（第 23-24 行）
  - 使用 `COALESCE` 优先读取新字段，fallback 旧字段
  - 同时显示新旧字段（兼容）

### 前端 Vue

#### 7. `frontend-vue/src/utils/predictionMapper.ts`
**修改位置：**
- `PredictionData` 接口（第 15-16 行）
  - 添加新字段 `pVulnRelated`, `isVulnRelated`
  - 保留旧字段 `pApplicable`, `applicable`（兼容）

- `getDisplayApplicableProb()` 函数（第 46-48 行）
  - 优先读取新字段 `pVulnRelated`，fallback 旧字段

- `getDisplayApplicable()` 函数（第 67-69 行）
  - 优先读取新字段 `isVulnRelated`，fallback 旧字段

#### 8. `frontend-vue/src/services/api.ts`
**修改位置：**
- `Prediction` 接口（第 63, 88-89 行）
  - `meta` 对象中添加新字段 `isVulnRelated`, `pVulnRelated`
  - 保留旧字段 `applicable`, `pApplicable`（兼容）
  - 顶层接口添加新字段，保留旧字段

#### 9. `frontend-vue/src/views/Predictions.vue`
**修改位置：**
- 表格列标签（第 51 行）
  - `P(Applicable)` → `P(Vuln-Related)`
  - tooltip 文本更新

- `getPApplicable()` 函数（第 660-670 行）
  - 优先读取新字段 `pVulnRelated`，fallback 旧字段

- `shouldShowSeverityInfo()` 函数（第 699-703 行）
  - 优先读取新字段 `isVulnRelated`，fallback 旧字段

#### 10. `frontend-vue/src/views/Report.vue`
**修改位置：**
- Applicability 显示（第 38-44 行）
  - 标签改为 `Vuln-Related`
  - 显示文本改为 `Vuln-Related` / `Not Vuln-Related`
  - 添加辅助函数 `getIsVulnRelated()`, `getPVulnRelated()`
  - 优先使用新字段，兼容旧字段

- `shouldShowSeverityInfo()` 计算属性（第 423-428 行）
  - 使用新辅助函数，优先新字段，兼容旧字段

## 未修改的内容

根据要求，以下内容**未修改**：
- ✅ 业务逻辑（阈值判断、条件分支）
- ✅ 模型推理流程
- ✅ 模型文件路径和训练逻辑
- ✅ 数据库表结构（字段存储在 JSON metadata 中）
- ✅ 错误处理逻辑

## 验证检查

### ✅ 已确认

1. **所有返回字典同时包含新旧字段**
   - ✅ `ml-service/app.py` 所有返回语句
   - ✅ `ml-service/risk.py` 所有返回语句

2. **所有读取逻辑优先新字段，fallback 旧字段**
   - ✅ `backend-node/src/services/prediction.service.ts` 所有提取逻辑
   - ✅ `frontend-vue` 所有读取逻辑

3. **导出功能使用新字段名**
   - ✅ CSV 导出表头更新
   - ✅ Excel 导出列名更新

4. **前端显示使用新字段名和文案**
   - ✅ 表格列标签更新
   - ✅ 详情页标签更新
   - ✅ 显示文案更新（Vuln-Related / Not Vuln-Related）

5. **API 参数支持新旧字段名**
   - ✅ ML 服务端点同时接受新旧参数名
   - ✅ 后端请求同时发送新旧参数名

## 兼容性说明

### 向后兼容
- 所有旧字段名仍然存在且值与新字段相同
- 旧客户端可以继续使用 `pApplicable` 和 `applicable`
- 历史脚本可以继续使用 `app_model_path` 和 `app_threshold`
- 数据库中的旧 metadata 可以通过 fallback 逻辑正常读取

### 向前兼容
- 新客户端应优先使用新字段名 `pVulnRelated` 和 `isVulnRelated`
- 新 API 请求应优先使用 `relevance_model_path` 和 `relevance_threshold`
- 前端显示使用新字段名和文案

## 测试建议

1. **功能测试**
   - 单样本预测：验证新旧字段都存在且值相同
   - 批量预测：验证新旧字段都存在且值相同
   - 导出功能：验证 CSV/Excel 使用新字段名

2. **兼容性测试**
   - 使用旧字段名的 API 请求应正常工作
   - 读取历史数据应能正常 fallback 到旧字段

3. **前端测试**
   - 预测列表页显示新字段名
   - 预测详情页显示新文案
   - 所有条件判断逻辑正常工作

---

**状态**: ✅ 重构完成  
**兼容性**: ✅ 完全向后兼容  
**业务逻辑**: ✅ 未改变

