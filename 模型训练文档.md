# 模型训练详细文档

## 目录

1. [项目概述](#项目概述)
2. [环境准备](#环境准备)
3. [数据准备](#数据准备)
4. [训练脚本说明](#训练脚本说明)
5. [配置文件详解](#配置文件详解)
6. [训练流程](#训练流程)
7. [模型评估](#模型评估)
8. [模型注册与部署](#模型注册与部署)
9. [常见问题与解决方案](#常见问题与解决方案)
10. [最佳实践](#最佳实践)

---

## 项目概述

本项目是一个基于机器学习的漏洞风险评估系统，通过分析漏洞描述文本，预测漏洞的高风险概率，并结合CVSS评分计算综合风险评分。

### 核心功能

- **文本特征提取**：使用TF-IDF向量化技术将漏洞描述转换为数值特征
- **二分类预测**：预测漏洞是否为高风险（CVSS ≥ 7.0）
- **风险评分计算**：结合模型预测概率和CVSS评分计算综合风险评分
- **模型类型支持**：RandomForest 和 XGBoost
- **类别不平衡处理**：支持SMOTE过采样技术
- **概率校准**：使用Isotonic回归校准预测概率

### 技术栈

- **Python 3.10+**
- **scikit-learn**：机器学习框架
- **XGBoost**：梯度提升树模型
- **imbalanced-learn**：处理类别不平衡
- **pandas/numpy**：数据处理
- **joblib**：模型序列化

---

## 环境准备

### 1. 系统要求

- Python 3.10 或更高版本
- 至少 4GB RAM（推荐 8GB+）
- 足够的磁盘空间（模型文件通常 50-200MB）

### 2. 安装依赖

```bash
cd ml-service
pip install -r requirements.txt
```

### 3. 依赖包说明

主要依赖包及其用途：

| 包名 | 版本 | 用途 |
|------|------|------|
| scikit-learn | >=1.8.0,<2.0.0 | 机器学习核心库，提供模型和评估指标 |
| xgboost | >=2.0.0 | XGBoost梯度提升树模型 |
| imbalanced-learn | >=0.11.0 | SMOTE过采样技术 |
| pandas | >=2.0.0 | 数据处理和分析 |
| numpy | >=1.24.0,<2.0.0 | 数值计算 |
| joblib | >=1.3.0 | 模型序列化和加载 |

### 4. Docker环境（推荐）

如果使用Docker Compose部署，所有依赖已包含在容器中：

```bash
# 启动所有服务
docker-compose up -d

# 进入ML服务容器
docker-compose exec ml-service bash
```

---

## 数据准备

### 1. 数据格式要求

训练数据应为CSV格式，包含以下字段：

#### 必需字段

- **`description_clean`** (或 `text_description`)：漏洞描述文本，用于特征提取
- **`cvss_base`** 或 **`cvss_severity`**：用于构建标签（至少有一个）

#### 可选字段

- **`cve_id`**：CVE编号（用于标识）
- **`cvss_base_score`**：CVSS基础评分（0-10）
- **`cvss_severity`**：严重程度（LOW/MEDIUM/HIGH/CRITICAL）

### 2. 标签构建规则

系统会自动从CVSS评分构建二分类标签：

```python
# 规则1：使用cvss_base
if cvss_base >= 7.0:
    label = 1  # 高风险
else:
    label = 0  # 低风险

# 规则2：如果cvss_base缺失，使用cvss_severity
if cvss_severity in ['HIGH', 'CRITICAL']:
    label = 1
elif cvss_severity in ['MEDIUM', 'LOW', 'NONE']:
    label = 0
```

### 3. 数据质量检查

在训练前，建议检查：

- **文本完整性**：确保描述文本不为空
- **标签分布**：检查高风险和低风险样本的比例
- **缺失值**：系统会自动处理缺失的文本（填充为空字符串）

### 4. 数据示例

```csv
cve_id,description_clean,cvss_base,cvss_severity
CVE-2024-0001,"A remote code execution vulnerability exists in the web server component. An attacker can exploit this by sending a specially crafted HTTP request.",9.8,CRITICAL
CVE-2024-0002,"Cross-site scripting (XSS) vulnerability allows an attacker to inject malicious scripts into web pages.",6.1,MEDIUM
```

---

## 训练脚本说明

项目提供两个训练脚本，推荐使用 `train_risk_model.py`。

### 1. train_risk_model.py（推荐）

**位置**：`ml-service/train_risk_model.py`

**特点**：
- 完整的风险导向训练流程
- 支持概率校准（Isotonic回归）
- 自动生成训练集预测结果
- 计算风险评分和风险等级
- 生成详细的预处理报告

**使用方式**：
```bash
python train_risk_model.py <config_json>
```

**示例**：
```bash
python train_risk_model.py train_config.json
```

### 2. train.py（基础版本）

**位置**：`ml-service/train.py`

**特点**：
- 基础的训练功能
- 支持RandomForest和XGBoost
- 可选SMOTE处理

**使用方式**：
```bash
python train.py <config_json>
```

---

## 配置文件详解

训练通过JSON配置文件控制所有参数。配置文件应包含以下字段：

### 完整配置示例

```json
{
  "csv_path": "./merged_json_table.csv",
  "model_type": "XGBoost",
  "use_smote": false,
  "test_size": 0.2,
  "random_state": 42,
  "output_dir": "./models/risk_model_001",
  "alpha": 0.6,
  "text_column": "description_clean",
  "max_features": 20000,
  "calibrate": true
}
```

### 配置参数说明

| 参数 | 类型 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `csv_path` | string | ✅ | - | 训练数据CSV文件路径 |
| `model_type` | string | ✅ | - | 模型类型：`"RandomForest"` 或 `"XGBoost"` |
| `output_dir` | string | ✅ | - | 模型输出目录 |
| `use_smote` | boolean | ❌ | false | 是否使用SMOTE处理类别不平衡 |
| `test_size` | float | ❌ | 0.2 | 测试集比例（0.0-1.0） |
| `random_state` | integer | ❌ | 42 | 随机种子，确保结果可复现 |
| `alpha` | float | ❌ | 0.6 | 风险评分中ML概率的权重（0.0-1.0） |
| `text_column` | string | ❌ | "description_clean" | 文本特征列名 |
| `max_features` | integer | ❌ | 20000 | TF-IDF最大特征数 |
| `calibrate` | boolean | ❌ | true | 是否进行概率校准（仅train_risk_model.py） |

### 预设配置文件

项目包含三个预设配置文件：

1. **`train_config.json`**：基础XGBoost配置，不使用SMOTE
2. **`train_with_smote_config.json`**：XGBoost + SMOTE配置
3. **`train_random_forest_config.json`**：RandomForest + SMOTE配置

### 模型类型选择指南

#### XGBoost
- **优点**：通常性能更好，训练速度快
- **适用场景**：数据量大，需要最佳性能
- **参数**：
  - `n_estimators`: 100
  - `max_depth`: 6
  - `learning_rate`: 0.1

#### RandomForest
- **优点**：更稳定，不易过拟合，特征重要性更可靠
- **适用场景**：数据量中等，需要稳定性和可解释性
- **参数**：
  - `n_estimators`: 100
  - `max_depth`: None（不限制深度）
  - `class_weight`: 'balanced'（自动平衡类别权重）

### SMOTE使用建议

**何时使用SMOTE**：
- 类别不平衡比例 > 3:1
- 少数类样本数 > 10（SMOTE需要至少2个样本）

**何时不使用SMOTE**：
- 数据已经平衡
- 少数类样本太少（< 10）
- 训练时间有限（SMOTE会增加训练时间）

---

## 训练流程

### 完整训练步骤

#### 步骤1：准备数据

确保训练数据CSV文件已准备好，包含必要的字段。

```bash
# 检查数据文件
ls -lh merged_json_table.csv
```

#### 步骤2：创建配置文件

创建或修改配置文件，设置训练参数。

```bash
# 复制预设配置
cp train_config.json my_train_config.json

# 编辑配置
vim my_train_config.json
```

#### 步骤3：执行训练

```bash
# 使用train_risk_model.py（推荐）
python train_risk_model.py my_train_config.json

# 或使用基础版本
python train.py my_train_config.json
```

#### 步骤4：检查训练结果

训练完成后，检查输出目录：

```bash
ls -lh models/risk_model_001/
```

应包含以下文件：
- `model.joblib` - 训练好的模型
- `vectorizer.joblib` - TF-IDF向量化器
- `metadata.json` - 模型元数据和性能指标
- `preprocessing_report.json` - 预处理报告（仅train_risk_model.py）
- `training_predictions.csv` - 训练集预测结果（仅train_risk_model.py）

### 训练过程输出说明

训练脚本会输出详细的进度信息：

```
============================================================
步骤1: 加载和预处理数据
============================================================
正在加载数据集: ./merged_json_table.csv
数据集形状: (10000, 15)
列名: ['cve_id', 'description_clean', 'cvss_base', ...]

开始预处理数据集...
处理缺失值...
构建风险标签...

标签分布:
  高风险 (y=1): 3500 (35.00%)
  低风险 (y=0): 6500 (65.00%)
  缺失标签: 0

============================================================
步骤2: 特征工程
============================================================
构建特征工程管道...
文本列: description_clean
最大特征数: 20000
特征矩阵形状: (10000, 20000)
特征数量: 20000

============================================================
步骤3: 数据集划分
============================================================
训练集大小: 8000
测试集大小: 2000

============================================================
步骤4: 应用SMOTE处理类别不平衡
============================================================
SMOTE后训练集大小: (13000, 20000), 类别分布: [6500 6500]

============================================================
步骤5: 模型训练
============================================================
训练XGBoost模型...
应用概率校准 (isotonic)...
开始训练...
训练完成！

性能指标:
  accuracy: 0.8750
  precision: 0.8234
  recall: 0.7891
  f1: 0.8059
  roc_auc: 0.9123

============================================================
步骤6: 保存模型
============================================================
模型已保存: ./models/risk_model_001/model.joblib
向量化器已保存: ./models/risk_model_001/vectorizer.joblib
元数据已保存: ./models/risk_model_001/metadata.json
预处理报告已保存: ./models/risk_model_001/preprocessing_report.json
训练预测结果已保存: ./models/risk_model_001/training_predictions.csv

============================================================
训练完成！
============================================================
模型输出目录: ./models/risk_model_001
```

### Docker环境训练

如果使用Docker Compose：

```bash
# 方法1：进入容器后训练
docker-compose exec ml-service bash
cd /app
python train_risk_model.py train_config.json

# 方法2：直接执行（不进入容器）
docker-compose exec ml-service python train_risk_model.py train_config.json
```

---

## 模型评估

### 评估指标说明

训练完成后，模型会计算以下性能指标：

| 指标 | 说明 | 理想值 |
|------|------|--------|
| **Accuracy** | 准确率：正确预测的样本比例 | > 0.80 |
| **Precision** | 精确率：预测为高风险中真正为高风险的比例 | > 0.75 |
| **Recall** | 召回率：所有高风险样本中被正确识别的比例 | > 0.75 |
| **F1 Score** | F1分数：精确率和召回率的调和平均 | > 0.75 |
| **ROC-AUC** | ROC曲线下面积：模型区分能力 | > 0.85 |

### 查看评估结果

训练完成后，评估指标保存在 `metadata.json` 中：

```bash
# 查看元数据
cat models/risk_model_001/metadata.json | python -m json.tool
```

示例输出：

```json
{
  "model_type": "XGBoost",
  "metrics": {
    "accuracy": 0.8750,
    "precision": 0.8234,
    "recall": 0.7891,
    "f1": 0.8059,
    "roc_auc": 0.9123
  },
  "class_distribution": {
    "class_0_count": 6500,
    "class_1_count": 3500,
    "imbalance_ratio": 1.86
  },
  "n_features": 20000,
  "n_train_samples": 8000,
  "n_test_samples": 2000
}
```

### 特征重要性分析

模型会输出Top 20最重要的特征：

```json
{
  "feature_importance": [
    {"name": "remote code execution", "importance": 0.0234},
    {"name": "buffer overflow", "importance": 0.0198},
    {"name": "sql injection", "importance": 0.0176},
    ...
  ]
}
```

### 训练集预测结果分析

`train_risk_model.py` 会生成训练集的预测结果文件 `training_predictions.csv`：

```csv
cve_id,P(vuln),RiskScore,RiskLevel,cvss_base
CVE-2024-0001,0.9234,0.9123,Critical,9.8
CVE-2024-0002,0.3456,0.3421,Low,6.1
...
```

可以用于：
- 分析模型在训练集上的表现
- 识别误分类样本
- 验证风险评分计算

---

## 模型注册与部署

### 1. 模型注册到数据库

训练完成后，需要将模型注册到系统数据库：

```bash
# 进入后端容器
docker-compose exec backend-node bash

# 注册模型（不激活）
node scripts/register-model.js /app/models/risk_model_001 risk_model_001

# 注册并激活模型
node scripts/register-model.js /app/models/risk_model_001 risk_model_001 --activate
```

### 2. 验证模型注册

在前端访问 **Models** 页面，应该能看到新注册的模型。

### 3. 激活模型

只有激活的模型才会用于预测。可以通过以下方式激活：

- **前端界面**：在Models页面点击"激活"按钮
- **命令行**：使用 `--activate` 参数注册时自动激活
- **API调用**：`POST /api/models/:id/activate`

### 4. 模型文件结构

注册的模型目录应包含：

```
models/risk_model_001/
├── model.joblib              # 训练好的模型
├── vectorizer.joblib         # TF-IDF向量化器
├── metadata.json             # 模型元数据
├── preprocessing_report.json  # 预处理报告（可选）
└── training_predictions.csv  # 训练预测结果（可选）
```

---

## 常见问题与解决方案

### 1. 训练失败：找不到数据文件

**错误信息**：
```
FileNotFoundError: [Errno 2] No such file or directory: './merged_json_table.csv'
```

**解决方案**：
- 检查CSV文件路径是否正确
- 使用绝对路径或相对于脚本执行目录的路径
- 在Docker环境中，确保文件在容器内可访问

### 2. 训练失败：缺少必要字段

**错误信息**：
```
ValueError: 缺少必要的列: ['description_clean']
```

**解决方案**：
- 检查CSV文件是否包含 `description_clean` 或 `text_description` 列
- 在配置文件中指定正确的 `text_column` 参数

### 3. 训练失败：没有有效标签

**错误信息**：
```
ValueError: 没有有效的标签数据！
```

**解决方案**：
- 确保数据包含 `cvss_base` 或 `cvss_severity` 字段
- 检查CVSS值是否在有效范围内（0-10）
- 检查严重程度值是否为：LOW, MEDIUM, HIGH, CRITICAL

### 4. SMOTE失败

**错误信息**：
```
SMOTE失败: Expected n_neighbors <= n_samples
```

**解决方案**：
- 少数类样本太少（< 2），无法使用SMOTE
- 在配置文件中设置 `"use_smote": false`
- 或增加训练数据量

### 5. 内存不足

**错误信息**：
```
MemoryError: Unable to allocate array
```

**解决方案**：
- 减少 `max_features` 参数（如从20000降到10000）
- 使用稀疏矩阵（系统会自动处理）
- 增加系统内存或使用更大内存的机器

### 6. 训练时间过长

**可能原因**：
- 数据量太大
- `max_features` 设置过高
- 使用SMOTE增加了数据量

**解决方案**：
- 减少 `max_features`
- 使用数据采样（训练前）
- 考虑使用XGBoost（通常比RandomForest快）

### 7. 模型性能不佳

**可能原因**：
- 数据质量差
- 类别极度不平衡
- 特征提取不当

**解决方案**：
- 检查数据质量，确保描述文本有意义
- 启用SMOTE处理类别不平衡
- 调整 `max_features` 和TF-IDF参数
- 尝试不同的模型类型

---

## 最佳实践

### 1. 数据准备

- ✅ **数据清洗**：移除重复样本，处理缺失值
- ✅ **文本质量**：确保描述文本完整、有意义
- ✅ **标签平衡**：尽量保持类别比例在合理范围（1:3 到 3:1）
- ✅ **数据量**：建议至少1000个样本，理想情况10000+

### 2. 参数调优

- ✅ **特征数量**：根据数据量调整 `max_features`
  - 小数据集（< 5000）：5000-10000
  - 中等数据集（5000-50000）：10000-20000
  - 大数据集（> 50000）：20000-50000

- ✅ **测试集比例**：通常使用0.2（20%），数据量大时可降至0.1

- ✅ **随机种子**：固定 `random_state` 确保结果可复现

### 3. 模型选择

- **XGBoost**：追求最佳性能，数据量大
- **RandomForest**：需要稳定性和可解释性，数据量中等

### 4. 类别不平衡处理

- 不平衡比例 < 2:1：通常不需要SMOTE
- 不平衡比例 2:1 到 5:1：建议使用SMOTE
- 不平衡比例 > 5:1：必须使用SMOTE，并考虑其他技术（如类别权重）

### 5. 模型验证

- ✅ **交叉验证**：对于小数据集，考虑使用交叉验证
- ✅ **独立测试集**：保留独立的测试集，不用于训练
- ✅ **业务指标**：除了技术指标，还要关注业务相关指标（如高风险召回率）

### 6. 模型管理

- ✅ **版本控制**：为每个模型版本创建独立目录
- ✅ **元数据记录**：确保 `metadata.json` 包含完整的训练信息
- ✅ **文档记录**：记录训练参数、数据来源、性能指标

### 7. 生产部署

- ✅ **模型验证**：部署前在测试环境验证模型性能
- ✅ **A/B测试**：新模型上线时进行A/B测试
- ✅ **监控**：监控模型在生产环境的预测性能
- ✅ **定期重训练**：根据新数据定期重新训练模型

### 8. 性能优化

- ✅ **特征缓存**：对于重复预测，缓存TF-IDF向量
- ✅ **批量预测**：使用批量预测API提高效率
- ✅ **模型压缩**：考虑使用模型压缩技术（如特征选择）

---

## 附录

### A. 训练脚本对比

| 特性 | train.py | train_risk_model.py |
|------|----------|---------------------|
| 概率校准 | ❌ | ✅ |
| 风险评分计算 | ❌ | ✅ |
| 训练集预测 | ❌ | ✅ |
| 预处理报告 | ❌ | ✅ |
| 数据格式 | JSON | CSV |
| 推荐使用 | 基础场景 | 生产环境 |

### B. 风险评分公式

```
如果存在CVSS评分：
    RiskScore = α × P(vuln) + (1 - α) × (CVSS / 10.0)
否则：
    RiskScore = P(vuln)
```

其中：
- `α`：ML概率权重（默认0.6）
- `P(vuln)`：模型预测的高风险概率 [0, 1]
- `CVSS`：CVSS基础评分 [0, 10]

### C. 风险等级映射

| RiskScore | 风险等级 |
|-----------|----------|
| < 0.40 | Low |
| 0.40 - 0.59 | Medium |
| 0.60 - 0.79 | High |
| ≥ 0.80 | Critical |

### D. 快速参考命令

```bash
# 训练XGBoost模型（不使用SMOTE）
python train_risk_model.py train_config.json

# 训练XGBoost模型（使用SMOTE）
python train_risk_model.py train_with_smote_config.json

# 训练RandomForest模型（使用SMOTE）
python train_risk_model.py train_random_forest_config.json

# Docker环境训练
docker-compose exec ml-service python train_risk_model.py train_config.json

# 注册模型
docker-compose exec backend-node node scripts/register-model.js /app/models/risk_model_001 risk_model_001 --activate

# 查看模型元数据
cat models/risk_model_001/metadata.json | python -m json.tool
```

---

## 更新日志

- **2024-01**: 初始版本，支持XGBoost和RandomForest
- **2024-02**: 添加SMOTE支持和概率校准
- **2024-03**: 添加风险评分计算和训练集预测

---

## 联系与支持

如有问题或建议，请：
1. 查看项目README文档
2. 检查常见问题部分
3. 查看代码注释和日志输出

---

**文档版本**: 1.0  
**最后更新**: 2024年

