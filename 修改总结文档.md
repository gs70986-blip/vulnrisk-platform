# 系统修改总结文档

## 一、修改文件清单

### 后端/ML服务
1. **ml-service/app.py** - 将hard clip改为soft degrade，新增字段
2. **backend-node/src/services/prediction.service.ts** - 传递新字段到数据库和前端

### 前端
3. **frontend-vue/src/services/api.ts** - 更新Prediction接口类型定义
4. **frontend-vue/src/views/Report.vue** - 重构报告页面
5. **frontend-vue/src/config/riskTaxonomy.ts** - 新增：风险类型分类体系
6. **frontend-vue/src/services/riskEvidence.ts** - 新增：风险证据提取服务

---

## 二、核心变更摘要

### 1. ML服务 (ml-service/app.py)

#### 变更点1：Soft Degrade逻辑替换Hard Clip
**位置**：`/predict` 端点，约344-372行

**原逻辑**（Hard Clip）：
```python
if not applicability['applicable']:
    p_vuln = 0.0
    risk_score = 0.0
    risk_level = 'N/A'
    explanation = 'Input text not vulnerability-related; scoring not applicable.'
```

**新逻辑**（Soft Degrade）：
```python
if not applicability['applicable']:
    is_clipped = True
    reliability = "Low"
    warnings.append('Insufficient evidence for high-severity scoring.')
    # 继续计算risk_score，不强制置0
    risk_score = calculate_risk_score(p_vuln, cvss_base_score, alpha=alpha)
    # riskLevel不再为N/A，改为Low或Unknown
    risk_level = 'Low' if risk_score < 0.4 else 'Unknown'
    explanation = 'Assessment has low confidence due to insufficient evidence...'
```

#### 变更点2：新增返回字段
**位置**：返回结果构建，约373-397行

**新增字段**：
- `p_vuln_raw`: 原始模型预测值（永远保留）
- `is_clipped`: 是否被裁剪/降级
- `reliability`: 可靠性（High/Medium/Low）
- `warnings`: 警告信息列表

#### 变更点3：Explanation文案修正
**禁止出现**："not vulnerability-related"

**改为强调**：
- "low confidence due to insufficient evidence"
- "Results are for reference only"
- "should be interpreted with caution"

---

### 2. 后端服务 (backend-node/src/services/prediction.service.ts)

#### 变更点1：提取新字段并存储
**位置**：`predict` 方法，约44-67行

```typescript
const { 
  p_vuln_raw,      // 新增
  is_clipped,      // 新增
  reliability,     // 新增
  warnings         // 新增
} = response.data;

// 构建扩展的metadata
const extendedMeta = {
  ...(meta || {}),
  p_vuln_raw,
  is_clipped: is_clipped || false,
  reliability: reliability || 'Medium',
  warnings: warnings || [],
};

// 存储时映射N/A为Unknown
riskLevel: risk_level === 'N/A' ? 'Unknown' : risk_level
```

#### 变更点2：返回时添加新字段
**位置**：`predict` 方法返回，约67行

```typescript
return {
  ...prediction,
  pVulnRaw: p_vuln_raw,
  isClipped: is_clipped || false,
  reliability: reliability || 'Medium',
  warnings: warnings || [],
  meta: extendedMeta,
};
```

#### 变更点3：查询时提取新字段
**位置**：`getPredictionById` 和 `getPredictions` 方法

从metadata中提取新字段并添加到响应中。

---

### 3. 前端API类型 (frontend-vue/src/services/api.ts)

#### 变更：Prediction接口扩展
```typescript
export interface Prediction {
  // ... 原有字段
  pVulnRaw?: number              // 新增
  isClipped?: boolean            // 新增
  reliability?: 'High' | 'Medium' | 'Low'  // 新增
  warnings?: string[]            // 新增
  meta?: {
    // ... 原有字段
    p_vuln_raw?: number          // 新增
    is_clipped?: boolean         // 新增
    reliability?: string         // 新增
    warnings?: string[]          // 新增
  }
}
```

---

### 4. 前端报告页面 (frontend-vue/src/views/Report.vue)

#### 变更点1：字段展示语义修正
- **不再优先展示pVuln**，改为展示`pVulnRaw`（标签改为"High-Severity Similarity (Raw)"）
- **显示isClipped标签**：如果isClipped=true，显示"Low Confidence"或"Clipped"标签
- **显示Reliability Badge**：来自reliability字段或前端估算
- **riskLevel映射**：如果为N/A，前端强制映射为"Unknown"，并显示说明

#### 变更点2：去重 - 删除Risk Breakdown图表
**删除**：原有的Risk Breakdown柱状图（P(vuln)/CVSS/riskScore的重复展示）

**替换为**："Reliability & Notes"模块，包含：
- reliability说明
- warnings展示
- explanation展示
- 技术细节（max_similarity, nonzero_features）

#### 变更点3：替换Top Features Importance
**删除**：全局top features importance图表（单样本报告不适用）

**替换为**："Evidence in Text"模块：
- 基于关键词匹配识别风险类型（XSS、RCE、Injection等）
- 显示匹配的风险类型标签
- 可折叠的证据列表（关键词+上下文片段+解释+推荐操作）
- 如果未命中，显示"Unknown + evidence insufficient"提示

#### 变更点4：新增Method Note折叠区
在页面底部添加Element Plus Collapse：
- 说明系统预测的是风险/严重性，不是检测漏洞是否存在
- 说明pvulnRaw是统计相似度，不是真实概率
- 说明输入缺少细节时可靠性会降低

---

### 5. 新增文件：风险证据提取

#### frontend-vue/src/config/riskTaxonomy.ts
定义8种风险类型：
- XSS (Cross-Site Scripting)
- RCE (Remote Code Execution)
- Injection (SQL/NoSQL/LDAP等)
- Privilege Escalation
- Authentication & Brute Force
- Information Disclosure
- DoS (Denial of Service)
- Misconfiguration

每种类型包含：
- keywords: 关键词列表
- explanation: 解释说明
- recommendedActions: 推荐操作

#### frontend-vue/src/services/riskEvidence.ts
提供函数：
- `extractRiskEvidence(text)`: 从文本中提取风险证据
- `highlightKeywords(text, keywords)`: 高亮关键词
- `getAllMatchedKeywords(text)`: 获取所有匹配关键词

---

## 三、XSS示例的新响应

### 输入
```json
{
  "sample_id": "test_xss_001",
  "text_description": "Cross-site scripting (XSS) vulnerability allows an attacker to inject malicious scripts into web pages viewed by other users. The application does not properly sanitize user input before displaying it."
}
```

### ML服务响应（新格式）
```json
{
  "sample_id": "test_xss_001",
  "p_vuln": 0.15,
  "p_vuln_raw": 0.15,
  "risk_score": 0.15,
  "risk_level": "Low",
  "cvss_base_score": null,
  "cvss_estimated": false,
  "cvss_method": "p_vuln_fallback",
  "is_clipped": true,
  "reliability": "Low",
  "warnings": [
    "Model probability is very low (0.150 < 0.100). Insufficient evidence for high-severity scoring."
  ],
  "explanation": "Model probability is low (0.150). The assessment has low confidence. Results are for reference only and should be interpreted with caution.",
  "meta": {
    "applicable": false,
    "reason": "LOW_PVULN",
    "max_similarity": 0.12,
    "nonzero_features": 45,
    "text_len": 185,
    "thresholds": {
      "clip_pvuln_threshold": 0.10,
      "clip_sim_threshold": 0.18,
      "clip_min_text_len": 20,
      "clip_min_nonzero_tfidf": 3
    },
    "p_vuln_raw": 0.15,
    "is_clipped": true,
    "reliability": "Low",
    "warnings": ["Model probability is very low (0.150 < 0.100). Insufficient evidence for high-severity scoring."]
  }
}
```

### 前端页面展示的关键字段映射

| 字段 | 展示位置 | 展示内容 |
|------|----------|----------|
| `pVulnRaw` (0.15) | Prediction Details | "High-Severity Similarity (Raw): 15.00%" + "Low Confidence"标签 |
| `reliability` ("Low") | Prediction Details + Reliability & Notes | Reliability Badge (黄色) + 说明文字 |
| `warnings` | 页面顶部 | 黄色Alert提示框 |
| `riskLevel` ("Low") | Prediction Details | Risk Level标签（绿色） |
| `explanation` | Reliability & Notes | 完整说明文字 |
| `isClipped` (true) | Prediction Details | "Low Confidence"标签 |
| Risk Types | Evidence in Text | "Cross-Site Scripting (XSS)"标签（红色） |
| Evidence | Evidence in Text | 可折叠的证据列表，包含关键词"xss"、"cross-site scripting"的上下文 |

---

## 四、验收检查清单

### ✅ 必须满足的条件

1. **页面不会显示N/A空结果**
   - ✅ riskLevel如果是N/A，前端映射为Unknown
   - ✅ 即使pVuln很低，也会显示数值和Low标签

2. **显示pVulnRaw（哪怕很低）**
   - ✅ 优先展示pVulnRaw，标签为"High-Severity Similarity (Raw)"
   - ✅ 如果pVulnRaw不存在，回退到pVuln

3. **显示Reliability和warnings**
   - ✅ Reliability Badge显示在Prediction Details中
   - ✅ warnings以Alert形式显示在页面顶部
   - ✅ Reliability说明显示在Reliability & Notes模块中

4. **Risk Types识别**
   - ✅ XSS文本能识别出"Cross-Site Scripting (XSS)"风险类型
   - ✅ 显示匹配的关键词和上下文

5. **explanation不出现"not vulnerability-related"**
   - ✅ 所有explanation文案都强调"low confidence"、"insufficient evidence"
   - ✅ 不再否定漏洞相关性

6. **系统演示不会翻车**
   - ✅ 明显漏洞文本（如XSS）不会被显示成N/A
   - ✅ 即使置信度低，也会显示结果并提示低可信度
   - ✅ 报告页更有说服力，包含证据和推荐操作

---

## 五、关键代码片段

### ML服务：Soft Degrade核心逻辑
```python
# 不再强制置零，而是继续计算但标记低可信度
is_clipped = False
reliability = "High"
warnings = []

# 计算风险评分（始终计算，不强制置零）
risk_score = calculate_risk_score(p_vuln, cvss_base_score, alpha=alpha)

if not applicability['applicable']:
    is_clipped = True
    reliability = "Low"
    warnings.append('Insufficient evidence for high-severity scoring.')
    # riskLevel不再为N/A
    risk_level = 'Low' if risk_score < 0.4 else 'Unknown'
```

### 前端：字段展示逻辑
```typescript
// 优先使用pVulnRaw
const effectivePVulnRaw = computed(() => {
  return prediction.value?.pVulnRaw ?? prediction.value?.pVuln ?? 0
})

// riskLevel映射
const effectiveRiskLevel = computed(() => {
  const level = prediction.value?.riskLevel || 'Unknown'
  return level === 'N/A' ? 'Unknown' : level
})
```

### 前端：风险证据提取
```typescript
// 从文本中提取风险证据
if (prediction.value?.textDescription) {
  riskEvidence.value = extractRiskEvidence(prediction.value.textDescription)
}
```

---

## 六、测试建议

### 测试用例1：XSS漏洞文本
**输入**：`"Cross-site scripting (XSS) vulnerability allows an attacker to inject malicious scripts..."`

**预期结果**：
- ✅ pVulnRaw显示（即使很低）
- ✅ Reliability显示为Low（如果pVulnRaw < 0.1）
- ✅ warnings显示
- ✅ Risk Types识别出XSS
- ✅ Evidence显示关键词匹配
- ✅ riskLevel不为N/A

### 测试用例2：明显非漏洞文本
**输入**：`"This is a normal blog post about cooking recipes."`

**预期结果**：
- ✅ pVulnRaw显示（可能很低）
- ✅ Reliability显示为Low
- ✅ warnings显示"insufficient evidence"
- ✅ Risk Types显示"Unknown + evidence insufficient"
- ✅ riskLevel为Low或Unknown（不为N/A）
- ✅ explanation强调低置信度，不否定漏洞相关性

---

## 七、向后兼容性

### 保持向后兼容
1. **数据库schema**：使用metadata JSON字段存储新字段，无需迁移
2. **API响应**：保留原有字段，新增字段为可选
3. **前端显示**：如果新字段不存在，使用回退逻辑（如pVulnRaw回退到pVuln）

### 旧数据兼容
- 旧预测记录可能没有pVulnRaw字段，前端会回退到pVuln
- 旧预测记录的riskLevel可能为N/A，前端会映射为Unknown
- 旧预测记录可能没有reliability，前端会根据pVulnRaw估算

---

**文档版本**: 1.0  
**最后更新**: 2024年




