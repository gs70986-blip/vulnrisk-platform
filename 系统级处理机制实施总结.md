# 系统级处理机制实施总结

## 概述

本次修改在预测响应组装处添加了保守的系统级处理机制，确保运行时行为与研究结论保持一致。**未修改任何模型、训练数据或核心预测逻辑**。

## 修改文件列表

### 1. ML服务 (`ml-service/app.py`)

**修改位置**: `predict_two_stage()` 函数（预测响应组装处）

**添加的功能**:

1. **输入质量检查（precheck）** - `check_input_quality()`
   - 检测过短文本（<15字符）
   - 检测高度重复文本
   - 检测问候语/闲聊模式
   - 检测无意义字符重复
   - 触发时：`applicable=false`, `riskScore=null`, `reliability=Low`, 添加说明性note

2. **补丁/缓解文本检测** - `detect_patch_mitigation_text()`
   - 检测包含修复/缓解关键词的文本（fix, patch, mitigation, sanitize等）
   - 触发时：降级可靠性，添加说明note，标记`inputType='patch_mitigation'`

3. **不确定性评估** - `assess_prediction_uncertainty()`
   - 计算概率分布熵
   - 检测无明显优势类别的情况
   - 触发时：降级可靠性，添加不确定性说明note

4. **可靠性降级** - `downgrade_reliability()`
   - High → Medium → Low
   - 不改变预测结果，仅调整可靠性

5. **模型信息传递** - `modelInfo`
   - 添加`modelInfo`字段，包含applicability和severity模型标识

### 2. 后端服务 (`backend-node/src/services/prediction.service.ts`)

**修改位置**: `predict()` 方法（响应组装处）

**修改内容**:
- 从ML服务响应中提取并传递新字段：
  - `reliability`
  - `notes`
  - `inputType`
  - `modelInfo`

### 3. 前端接口定义 (`frontend-vue/src/services/api.ts`)

**修改内容**:
- 在`Prediction`接口中添加新字段：
  - `reliability?: 'High' | 'Medium' | 'Low'`
  - `notes?: string[] | null`
  - `inputType?: 'normal' | 'patch_mitigation' | 'low_quality'`
  - `modelInfo?: { applicabilityModel?: string, severityModel?: string }`

### 4. 预测列表页面 (`frontend-vue/src/views/Predictions.vue`)

**修改内容**:

1. **条件显示逻辑** - `shouldShowSeverityInfo()`
   - 当`applicable=false`或`riskLevel='N/A'`时，隐藏P(High+)、Risk Score、Severity Probabilities
   - 显示"N/A"占位符

2. **补丁/缓解标签**
   - 在Risk Level列显示"Remediation"警告标签（当`inputType='patch_mitigation'`时）

### 5. 报告页面 (`frontend-vue/src/views/Report.vue`)

**修改内容**:

1. **条件显示严重度信息** - `shouldShowSeverityInfo` computed
   - 当`applicable=false`或`riskLevel='N/A'`时，隐藏Severity Probabilities、P(High+)、Risk Score

2. **补丁/缓解警告横幅**
   - 当`inputType='patch_mitigation'`时，显示警告横幅

3. **Notes显示**
   - 显示后端返回的`notes`数组作为信息提示

4. **模型信息显示修复**
   - 显示Severity模型名称（而不是Applicability模型）
   - 同时显示两个模型的标识（如果使用两阶段模型）
   - 添加`getModelName()`函数从路径提取模型名称

## 验证测试用例

### 测试用例1: "hello hello, nice to meet you"
- **预期**: `applicable=false`, `riskScore=null`, `riskLevel='N/A'`, `reliability='Low'`
- **原因**: 输入质量检查检测到问候语模式

### 测试用例2: "TODAY IS A GOOD DAY XSS"
- **预期**: 可能通过applicability检查，但不确定性评估会降级可靠性
- **原因**: 文本结构异常，概率分布可能较平

### 测试用例3: "Fix: Prevent command injection..."
- **预期**: `inputType='patch_mitigation'`, 可靠性降级，显示Remediation标签
- **原因**: 补丁/缓解文本检测

### 测试用例4: 正常CVE漏洞描述
- **预期**: 完全正常的输出，所有字段正常显示
- **原因**: 不触发任何质量检查或降级机制

## 技术要点

1. **保守性原则**: 所有检查都是保守的，不会影响正常CVE或GitHub issue文本
2. **非破坏性**: 不改变模型输出，只添加元数据和可靠性标记
3. **向后兼容**: 所有新字段都是可选的，不影响现有API
4. **可解释性**: 所有降级和拒绝都有明确的说明性notes

## API兼容性

- ✅ 所有现有字段保持不变
- ✅ 新字段都是可选的
- ✅ 前端优雅降级（显示N/A或隐藏字段）

## 论文答辩适用性

这些修改确保了：
1. **运行时行为一致性**: 系统在演示时不会对明显非漏洞文本给出高风险评分
2. **可解释性**: 所有决策都有明确的说明
3. **保守性**: 不会过度拒绝正常输入
4. **研究完整性**: 不改变模型训练或评估结果

---

**实施日期**: 2026-02-04  
**修改者**: AI Assistant  
**验证状态**: 待测试


