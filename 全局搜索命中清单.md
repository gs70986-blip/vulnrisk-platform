# 全局搜索命中清单

**生成时间**: 2026-02-03  
**搜索范围**: ml-service/, backend-node/, frontend-vue/

---

## 📂 ml-service/ 搜索结果

### 1. train_risk_model.py
**文件路径**: `ml-service/train_risk_model.py`  
**关键函数/类**:
- `build_feature_pipeline()` - 构建TF-IDF特征工程管道
- `train_model()` - 训练XGBoost或RandomForest模型
- `main()` - 主训练流程入口

**说明**: 风险导向的漏洞预测模型训练脚本，支持XGBoost和Random Forest，包含概率校准和SMOTE处理

---

### 2. TfidfVectorizer
**文件路径**: `ml-service/train_risk_model.py:15, 61`  
**关键函数/类**:
- `TfidfVectorizer()` - scikit-learn的TF-IDF向量化器

**说明**: 用于将文本描述转换为数值特征，配置参数：max_features=20000, ngram_range=(1,2), min_df=2, max_df=0.95

---

### 3. SMOTE
**文件路径**: `ml-service/train_risk_model.py:21, 285-286`  
**关键函数/类**:
- `SMOTE()` - imbalanced-learn的SMOTE过采样类
- `smote.fit_resample()` - 应用SMOTE处理类别不平衡

**说明**: 用于处理类别不平衡问题，通过合成少数类样本来平衡训练集，仅在训练集上应用

---

### 4. CalibratedClassifierCV
**文件路径**: `ml-service/train_risk_model.py:16, 126`  
**关键函数/类**:
- `CalibratedClassifierCV()` - scikit-learn的概率校准包装器

**说明**: 使用Isotonic回归进行概率校准，提高预测概率的准确性，使用3折交叉验证

---

### 5. clip_ (工程裁剪相关)
**文件路径**: `ml-service/app.py:38-42, 336-340`  
**关键变量/函数**:
- `CLIP_NA_ENABLED` - 是否启用裁剪机制
- `CLIP_PVULN_THRESHOLD` - 裁剪概率阈值（默认0.10）
- `CLIP_SIM_THRESHOLD` - 裁剪相似度阈值（默认0.18）
- `CLIP_MIN_TEXT_LEN` - 最小文本长度（默认20）
- `CLIP_MIN_NONZERO_TFIDF` - 最小非零特征数（默认3）
- `assess_applicability()` - 评估输入文本是否适用于风险评估

**说明**: 工程裁剪（Business Clipping）机制，防止无关文本被错误评分，包含多个阈值配置

---

### 6. similarity (相似度计算)
**文件路径**: `ml-service/risk.py:62-163`  
**关键函数**:
- `estimate_cvss_from_similarity()` - 基于TF-IDF余弦相似度估算CVSS评分
- `cosine_similarity()` - 计算余弦相似度

**说明**: 使用训练数据的文本相似度来估算CVSS评分，基于top-k最相似样本的加权平均，相似度阈值0.18

---

### 7. p_vuln (漏洞概率)
**文件路径**: 
- `ml-service/app.py:245, 261-291, 411-412`
- `ml-service/risk.py:10, 18, 26`
- `ml-service/train_risk_model.py:314`

**关键变量/函数**:
- `p_vuln` - 调整后的漏洞概率（经过置信度调整）
- `p_vuln_raw` - 原始模型预测概率（未调整）
- `calculate_risk_score(p_vuln, ...)` - 使用p_vuln计算风险评分

**说明**: 核心预测变量，表示模型预测的高风险漏洞概率[0,1]，经过多层次置信度调整机制处理

---

### 8. risk_level (风险等级)
**文件路径**: 
- `ml-service/risk.py:42-59`
- `ml-service/app.py:383-393, 414`

**关键函数**:
- `get_risk_level(risk_score)` - 根据风险评分映射到风险等级

**说明**: 将风险评分[0,1]映射到四个等级：Low(<0.4), Medium(0.4-0.7), High(0.7-0.9), Critical(≥0.9)

---

## 📂 backend-node/ 搜索结果

### 1. /api/models/train
**文件路径**: `backend-node/src/routes/models.ts:11`  
**关键路由**:
- `router.post('/train', modelController.trainModel)` - POST /api/models/train

**说明**: 模型训练API端点，接收训练请求并调用ML服务进行模型训练

---

### 2. /api/predictions
**文件路径**: `backend-node/src/routes/predictions.ts:55-60`  
**关键路由**:
- `router.post('/', predictionController.predict)` - POST /api/predictions (单样本预测)
- `router.post('/batch', predictionController.batchPredict)` - POST /api/predictions/batch (批量预测)
- `router.post('/batch/upload', ...)` - POST /api/predictions/batch/upload (文件上传批量预测)
- `router.get('/export', predictionController.exportPredictions)` - GET /api/predictions/export (导出预测结果)
- `router.get('/', predictionController.getPredictions)` - GET /api/predictions (获取预测列表)
- `router.get('/:id', predictionController.getPredictionById)` - GET /api/predictions/:id (获取单个预测)

**说明**: 预测相关的所有API端点，支持单样本、批量、文件上传和导出功能

---

### 3. ML_SERVICE_URL
**文件路径**: 
- `backend-node/src/config/index.ts:8`
- `backend-node/src/services/prediction.service.ts` (多处使用)

**关键变量**:
- `mlServiceUrl: process.env.ML_SERVICE_URL || 'http://localhost:5000'`

**说明**: ML服务的URL配置，用于后端调用ML服务的API，默认值为http://localhost:5000

---

## 📂 frontend-vue/ 搜索结果

### 1. pVuln
**文件路径**: 
- `frontend-vue/src/views/Report.vue:41-45, 262-264, 273-274`
- `frontend-vue/src/views/Predictions.vue:52-56`
- `frontend-vue/src/services/api.ts:78-79`

**关键变量/计算属性**:
- `effectivePVulnRaw` - 计算属性，优先使用pVulnRaw，回退到pVuln
- `prediction.pVuln` - 预测结果中的漏洞概率
- `prediction.pVulnRaw` - 原始预测概率（新增字段）

**说明**: 前端展示的漏洞概率值，在Report页面显示为"High-Severity Similarity (Raw)"，带进度条可视化

---

### 2. riskLevel
**文件路径**: 
- `frontend-vue/src/views/Report.vue:80-81, 283-285`
- `frontend-vue/src/views/Predictions.vue:70-79, 685-697`
- `frontend-vue/src/services/api.ts:85`

**关键变量/函数**:
- `effectiveRiskLevel` - 计算属性，将N/A映射为Unknown
- `getRiskTagType(riskLevel)` - 根据风险等级返回标签类型
- `sortRiskLevel()` - 风险等级排序函数

**说明**: 风险等级显示和排序，支持Low/Medium/High/Critical/Unknown，前端会将N/A映射为Unknown

---

### 3. Report.vue
**文件路径**: `frontend-vue/src/views/Report.vue`  
**关键组件/功能**:
- 预测报告详情页面组件
- `effectivePVulnRaw` - 显示原始预测概率
- `effectiveRiskLevel` - 显示风险等级
- `effectiveReliability` - 显示可靠性等级
- 风险证据提取和展示
- 警告信息展示

**说明**: 单个预测结果的详细报告页面，包含预测详情、风险等级、可靠性、证据提取等完整信息展示

---

### 4. Predictions.vue
**文件路径**: `frontend-vue/src/views/Predictions.vue`  
**关键组件/功能**:
- 预测结果列表页面组件
- 单样本预测对话框
- 批量预测文件上传
- 预测结果导出（CSV/Excel/JSON）
- 预测结果表格展示和排序
- `sortRiskLevel()` - 风险等级排序

**说明**: 预测结果管理页面，支持查看历史预测、单样本预测、批量导入、结果导出等功能

---

## 📊 统计摘要

### ml-service/ 命中统计
- **train_risk_model.py**: 1个文件，3个关键函数
- **TfidfVectorizer**: 1个文件，1个类
- **SMOTE**: 1个文件，1个类，2个方法调用
- **CalibratedClassifierCV**: 1个文件，1个类
- **clip_相关**: 1个文件，5个环境变量，1个函数
- **similarity相关**: 1个文件，2个函数
- **p_vuln相关**: 3个文件，多处使用
- **risk_level相关**: 2个文件，1个函数

### backend-node/ 命中统计
- **/api/models/train**: 1个路由文件，1个端点
- **/api/predictions**: 1个路由文件，6个端点
- **ML_SERVICE_URL**: 2个文件，1个配置变量

### frontend-vue/ 命中统计
- **pVuln**: 3个文件，多个计算属性和变量
- **riskLevel**: 3个文件，多个计算属性和函数
- **Report.vue**: 1个文件，完整报告页面组件
- **Predictions.vue**: 1个文件，预测管理页面组件

---

## 🔍 关键发现

### 1. 模型训练流程
- **训练脚本**: `train_risk_model.py` 是主要的训练入口
- **特征工程**: 使用 `TfidfVectorizer` 进行文本向量化
- **类别平衡**: 可选使用 `SMOTE` 处理类别不平衡
- **概率校准**: 使用 `CalibratedClassifierCV` 提高概率准确性

### 2. 预测流程
- **后端API**: `/api/predictions` 提供多种预测接口
- **ML服务**: 通过 `ML_SERVICE_URL` 调用ML服务
- **工程裁剪**: `clip_` 相关配置防止误报
- **相似度估算**: 使用 `similarity` 估算CVSS评分

### 3. 前端展示
- **Report.vue**: 详细的单样本报告页面
- **Predictions.vue**: 预测结果列表和管理页面
- **pVuln/riskLevel**: 核心展示字段，带可视化效果

---

**清单生成完成时间**: 2026-02-03



